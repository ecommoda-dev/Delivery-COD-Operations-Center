<div dir="rtl" style="text-align: right;">

# مركز عمليات الشحن والتحصيل — Delivery COD Operations Center (`Delivery-COD-Operations-Center`)

![version](https://img.shields.io/badge/version-v1.11.0-blue)

**بتعمل إيه:** هب واحد لمحطة الشحن والتحصيل. الموظف بيدخل **مرة واحدة**،
وبيشوف طوابير الشحن: اللي **جاهز يخرج**، واللي **خرج ولسه بلا نتيجة** —
**وبيعمل جرد للطرود اللي في المكتب بالسكانر** (تاب جوّه «قسم الجاهز للشحن») —
**وبيشتغل على تلات أدوات مدموجة تحت المركز**: **تحديث حالة الأوردرات**
و**تحصيل الأوردرات COD** (v1.5.0)، و**التسليم الجزئي** (v1.10.0).
**مين بيستخدمها:** الشحن والتحصيل
**الإصدار:** `v1.11.0` — **واحد للهب كله** (`TOOL_VERSION` في `shared/shell.js`)

> 🔴 **الريبو ده فيه النُصّين: الواجهة + Worker الدخول** (`index.js` ·
> `wrangler.toml`). وده **الشكل القياسي** مش استثناء — قرار ٨ في
> `ecommoda-tool-migration-playbook`: «أداة = Worker واحد + HTML واحد + ريبو
> واحد». الهب هو الأداة، وWorker الدخول جزء منه.
> ⛔ **وWorker الدخول مالوش أي endpoint تشغيلي** — لا طابور ولا سكان. أي
> أداة جديدة في المركز بتاخد **Worker بتاعها في ريبوها** (نفس قرار ٨).
> الاسم بيقول «مركز العمليات» فالإغراء موجود، والقاعدة دي هي اللي بتمنعه.
> ⚠️ والطابورين Workers مستقلين في ريبوهاتهم (`Ready-Orders` ·
> `Shipped-Orders`) — **مفيش Promote ليهم من هنا**.

> 🔴 **وبطاقة «التسليم الجزئي» في «قسم أدوات العمليات» بقت صفحة مدموجة
> جوّه الهب من v1.10.0 (`Partial-Delivery.html` — اسم الملف اتغيّر لاسم
> الريبو بالحرف في v1.11.0) — بدل الرابط الخارجي اللي كانت عليه.**
> التفاصيل الكاملة في قسم «الأدوات المدموجة» تحت.
> ⛔ **وعكس Order-Status-Updater.html/COD-Payment-Center.html، الريبو
> المستقل بتاعها (`Partial-Delivery`) بقى Worker وبس** — الصفحة اللي
> كانت فيه اتشالت خالص، فمفيش نسخة تانية شغّالة في مكان تاني ولا رابط
> قديم. أي تعديل على منطق الأداة (لا الشِل) بيتعمل في
> `Partial-Delivery.html` هنا مباشرة، وأي تعديل على الـ Worker بيتعمل في
> ريبو `Partial-Delivery`.
> ⛔ **ومفيش Promote للـ Worker من هنا** (نفس قرار ٨: Worker بتاعها في
> ريبوها). **وقيمة `tool = 'partial_delivery'` لسه مش مسجّلة في
> `ecommoda-constants` §7** — حاجز نشر مستقل عن أي حاجة في الهب ده.

> 🔴 **وتاب «جرد المكتب» (v1.3.0) مابيكسرش قاعدة «عرض بحت»** — الجرد كله
> **محلي على جهاز الموظف** (حالته في `sessionStorage`)، والنداء الزيادة
> الوحيد هو `lookup_orders` وهو **قراءة**. صفر كتابة على شوبيفاي وصفر صف
> في D1، فالـ binding لسه مالوش لازمة.
> ⛔ ولو اتطلب يوم **سجل جرد** في D1: `tool`/`type` يتسجّلوا في
> `ecommoda-constants` §7 **قبل** أول `writeLog` (Rule 7) — مش بعده.

> 🔴 **والطابورين دول عرض بحت في نسخة v1.0.0 (قرار أحمد 15-09-2026)** —
> مفيش أي فعل بيتكتب: لا تغيير حالة ولا إلغاء ولا تسجيل تحصيل. الـ Workers
> بتوعهم **قراءة بحتة** ومالهمش `[[d1_databases]]` أصلاً.
> ⛔ أول ما يتضاف أي فعل: الـ binding يترجع في `wrangler.toml`، و`tool`/`type`
> **يتسجّلوا في `ecommoda-constants` §7 قبل أول `writeLog`** (Rule 7) — والقاعدة
> دي اتخرقت **ست مرات** في الستاك ده، وكل مرة الادعاء كان مكتوب في `CLAUDE.md`.

> ⚠️ **كلمة «التحصيل» في الاسم مقصودة ومش سابقة لأوانها.** الهب ده هيستقبل
> أدوات تحصيل لاحقة (قرار أحمد)، والنسخة دي بتقول رقم التحصيل بس من غير ما
> تلمسه: **«مستحق التحصيل»** على كل طابور — مجموع `currentTotalPriceSet`
> للأوردرات اللي حالتها المالية `PENDING`.

## الروابط

```
الواجهة : https://ecommoda-dev.github.io/Delivery-COD-Operations-Center/
الـ Worker : https://delivery-cod-operations-center-worker.ecommoda-dev.workers.dev
tool في D1 : delivery_cod_ops_center   ← login · logout بس (ولسه مطلوب تسجيله)
مجموعة السر : delivery_cod_ops         ← 🔴 **مستقلة عن محطة المخزن** · **ستة Workers من v1.10.0**
مفتاح localStorage : delivery_cod_ops_worker_secret
مفتاح sessionStorage : dco_session     ← 🔴 **مفتاح مستقل** عن هب المخزن
```

> 🔴 **ومن v1.5.0 الهب بقى بيخدم أدوات كتابة جوّاه** (قرار أحمد
> 16-09-2026 · 17-09-2026): `Order-Status-Updater.html` و
> `COD-Payment-Center.html`، وانضمّ لهم `Partial-Delivery.html` في
> v1.10.0 (أسماء الملفات التلاتة اتغيّرت لاسم ريبوها بالحرف في v1.11.0).
> التفاصيل الكاملة في قسم
> «الأدوات المدموجة» تحت — والفرق الجوهري إن الطوابير **عرض بحت** والتلات
> أدوات دول **بيكتبوا**: حالة أوردر على شوبيفاي، وصفوف فلوس في D1، وحذف
> منتج من أوردر (Order Edit).

## الصفحات والـ Workers

| الصفحة | الأداة | الـ Worker | الحد الأدنى | Tier |
|---|---|---|---|---|
| `index.html` | الدخول + الشاشة الرئيسية | **`delivery-cod-operations-center-worker`** (الدخول) + الاتنين (للأعداد) | **`1.0.0`** | L (1400) |
| `ready-orders.html` | **قسم الجاهز للشحن** + 🧮 **تاب جرد المكتب** | `ready-orders-worker` | **`1.4.0`** | L (1400) |
| `shipped-orders.html` | **طابور المشحون** | `shipped-orders-worker` | **`1.1.0`** | L (1400) |
| `Order-Status-Updater.html` | 🔗 **تحديث حالة الأوردرات** (مدموجة · اسم الملف اتغيّر v1.11.0) | `order-status-updater-worker` | **`4.7.0`** | **M (1200)** |
| `COD-Payment-Center.html` | 🔗 **تحصيل الأوردرات COD** (مدموجة · اسم الملف اتغيّر v1.11.0) | `cod-payment-center-worker` | **`3.5.0`** | **M (1200)** |
| `Partial-Delivery.html` | 🔗 **التسليم الجزئي** (مدموجة من v1.10.0 · بلا نسخة قديمة · اسم الملف اتغيّر v1.11.0) | `partial-delivery-worker` | **`1.0.1`** | **M (1200)** |

> ⚠️ **تلات `min` مستقلة تمامًا** — مالهمش أي علاقة ببعض ولا بـ`TOOL_VERSION`.
> `min` مايترفعش إلا لما الهب **يعتمد فعلاً** على حاجة جديدة في الـ Worker ده
> (Standards #29). ترفيعه بلا سبب = تحذير كاذب على أي rollback مشروع.

> 🔴 **`ready.min = '1.4.0'` و`shipped.min = '1.1.0'` من v1.9.0 — أرضية
> تاريخ الطابور.** الـ Workers من النسختين دول **مابيجيبوش** أي أوردر أقدم
> من `01/04/2026`، وبيرجّعوا الأرضية في الرد (`minCreatedDay`) عشان الشاشة
> تكتبها جنب الرقم.
> ⛔ **وعلى Worker أقدم الطابور بيرجع بكامل الأوردرات القديمة** — يعني
> القرار اللي أحمد طلبه **مش مطبّق أصلاً**، والحاجة الوحيدة اللي بتقول كده
> على الشاشة هي **غياب** الشيب — وغياب حاجة مابيتلاحظش. فالحارس هو اللي
> بيسمّي الأداة والنسخة.
> ⚠️ **والتدهور محصور**: الطابور والجرد وكل الأعمدة شغّالين بالكامل — اللي
> بيقع هو **نطاق** الطابور بس.
> ✅ **والحارس ده هو اللي بيقول الحالة — مش الملف ده.** سكوته معناه إن الـ
> Workers على النسخة المطلوبة؛ وطلوعه معناه إنهم أقدم منها فعلاً.

> 🔴 **`ready.min = '1.3.0'` كان من v1.6.0 — عمود «ملحوظات» بيقرا `note`.**
> الحقل ده جه في `ready-orders-worker` **v1.3.0** من `get_ready_queue`
> (حقل **Notes** بتاع الأوردر على شوبيفاي). على Worker أقدم العمود بيقول
> `—` على **كل** صف — يعني «مفيش ولا ملحوظة في الطابور كله»، وده **ادعاء
> غلط** مش خانة فاضية: الموظف بيبطّل يبص على العمود خالص.
> ⚠️ **والتدهور محصور ومُعلَن**: الطابور والجرد وباقي الأعمدة شغّالين
> بالكامل، والحارس بيسمّي الأداة والنسخة المطلوبة.


> 🔴 **و`ready.min = '1.2.0'` كان من v1.3.0 — تاب الجرد بينادي `lookup_orders`.**
> قسم «موجودة خطأ» في الجرد بيقول **حالة الأوردر الحقيقية** (S1 · S2 ·
> الإلغاء)، ومصدرها الوحيد الـ endpoint ده في Worker `1.2.0`. على Worker
> أقدم النداء بيرجّع `404` والقسم بيفضل بيقول «حالته لسه مش معروفة».
> ⚠️ **والتدهور محصور ومُعلَن**: الجرد نفسه (سكان · مضبوطة · مفقودة ·
> تصدير) شغّال بالكامل بلا الـ endpoint ده — اللي بيقع هو **سبب** الصف
> التالت بس، والرسالة بتسمّي الأداة والنسخة المطلوبة بالنص.


> 🔴 **و`ready.min = '1.1.0'` كان أول حارس مشروع في الهب ده (v1.1.0).**
> `ready-orders.html` بقت معتمدة فعلاً على `address1`/`address2` في رد
> `get_ready_queue` (Worker v1.1.0): **عمود «العنوان»** مبني عليهم. على
> Worker `1.0.0` العمود بيقول **«بلا عنوان» على كل صف**، وعمود كامل بجملة
> تحذير بيتقري **عطل في الأداة** مش «Worker قديم» — فالحارس بيسمّي السبب
> (Standards #29).
> ⚠️ **والتدهور هنا مُعلَن مش صامت** — باقي الأعمدة شغّالة بالكامل والطابور
> بيتجاب عادي؛ اللي بيقع هو خانة العنوان لوحدها.
> 🔴 **`shipped.min` لسه `1.0.0` — ومفيش نسخة أقدم منشورة.** يعني الحارس
> هناك **مش** بيمنع rollback؛ هو بيمسك الحالة الوحيدة الممكنة: الـ Worker
> ما اتنشرش أصلاً أو الـ Promote ناقص، فـ`get_config` بيرجّع `404`/`401`
> والحارس **بيسمّي الأداة** بدل ما الموظف يدوّر.

> 🔴 **والهب مابقاش بينادي Worker التغليف خالص** (قرار أحمد 15-09-2026).
> الدخول بقى على Worker الهب نفسه، والنتيجة:
> · صفر تعديل في ريبو `Orders-Packing-Checker` — لا `AUTH_APPS` ولا Promote
>   على أكتر Worker شغّال في المخزن.
> · **صفر `appId`** — الـ Worker بيخدم واجهة واحدة، فاسم الأداة في D1
>   متحدّد في كوده مش جاي من العميل. ⛔ والفرق مهم: قيمة جاية من العميل
>   معناها أي طلب معاه السر يقدر يكتب صفوف بأي اسم أداة في `logs` المشترك.
> · وصف الدخول بيتكتب `delivery_cod_ops_center` **من أول سكانة** — يعني
>   الترتيب اللي Rule 7 بتطلبه (التسجيل قبل أول `writeLog`) **قابل للتحقيق**
>   بدل ما يتعكس زي الست مرات اللي فاتت في الستاك.

## 🔴 القرارات المعمارية — التلاتة اللي كل حاجة تانية اتبنت عليهم

### ① الاشتقاق كله في `shared/shell.js` §QUEUE-RULES — مش في الصفحة

الشاشة الرئيسية بتعرض **رقم** كل طابور، والصفحة بتعرض **قايمته**. لو القاعدة
اتكتبت في الصفحة، الرقمين بيفترقوا **في صمت**.

> 🔴 **وده مش خطر نظري — ده حصل في هب المخزن v1.11.0:** الرئيسية قالت «بوسطة
> ٦٦» والصفحة فتحت على **٦**، لأن بوابة العرض كانت مكتوبة في `print.html`
> لوحدها. الإصلاح كان نقل البوابة للـ shell (درس R1).

اللي عايش في §QUEUE-RULES: `dcoMachineOf` · `dcoFlags` · `dcoShapeRow` ·
`dcoQueueRows` · `dcoCod` · `dcoChips` · `dcoCourierGroup` · `DCO_COURIER_GROUPS`
· `dcoCourierCounts` · `dcoFlaggedCount` · `dcoMoney`.

> ⛔ **ممنوع أي صفحة تعرّف أي واحدة منهم تاني.** تعريف في صفحة **بيغلب** الـ
> shell (بيتحمّل قبلها) والقاعدتين هيفترقوا في صمت.
> ✅ وفيه بند في `docs/queues-check.mjs` بيقارن **الرقم في الرئيسية بعدد صفوف
> الصفحة فعليًا** — مش بيقرا الكود.

### ② «علّم، متشيلش» — الصف الشاذ بيفضل في الطابور

القاعدة ١٣ و١٤ في `ecommoda-order-lifecycle`: أي صف حالته غريبة **بياخد علامة
وبيفضل مكانه وبيتعدّ في الرقم**.

| العلامة | امتى | اللي بتقوله |
|---|---|---|
| `cancelled` | `cancelledAt` أو `S1 = Cancelled` | «ملغي» + الفعل المطلوب |
| `both_machines` | S1 و S2 على نفس القيمة | الصف محسوب على S1 |
| `s2_before_delivered` | S2 في الطابور و S1 مش `Delivered` | دورة استبدال بدري |
| `s1_unknown` · `s2_unknown` | قيمة بره قايمة الاختيار | القيمة الغلط بالحرف |
| `zone_missing` | `BLANK` أو فاضي | «بلا قناة» |
| `zone_unknown` | زون بره القايمة | القيمة الغلط بالحرف |
| `at_courier` (جاهز) | «موقع الشحنة» `Courier` والحالة لسه `Ready` | الشحنة خرجت ومحدش سجّل |
| `not_fulfilled` (مشحون) | `Shipped` و شوبيفاي `UNFULFILLED` | الشحنة مش مسجّلة |

- 🔴 **كل علامة بتقول تلات حاجات:** إيه اللي حصل · **القيمة الغلط بالحرف** ·
  **الفعل المطلوب**. بند مراجعة بيقول «فيه حاجة» وبس تكلفته **فحص يدوي لكل
  صف** على شوبيفاي (قاعدة ١٤).
- 🔴 **والصف مابيتشالش ومابينقلش.** نقله بيغيّر أرقام — وده قرار أحمد مش قرار
  الكود — وبيخلّي سؤال «الأوردر ده فين؟» بلا إجابة.
- ⚠️ **وخلفية الصف المعلّم خفيفة عن قصد** — لون قوي كان هيخلّيه يتقري **رفض**،
  وهو مش مرفوض: هو في الطابور وبيتعدّ، والعلامة معناها «راجعه».
- 🔴 **`not_fulfilled` على صفوف S1 بس.** أداة الشحن بتقفل الحالة على `Shipped`
  **وبتعمل فلفلمنت** في نفس الخطوة، فـ`UNFULFILLED` على S1 معناه إن الشحنة مش
  مسجّلة. أما عُرف الفلفلمنت في دورة S2 **مش مؤكَّد عندنا**، وعلامة غلط على
  **كل** صف استبدال أسوأ من مفيش علامة: الموظف بيتعلّم يعدّي على التحذير كله
  (نفس درس `already` الأحمر في سكانرات بوسطة).
  ⚠️ والفحص على `UNFULFILLED` **بالظبط** — `PARTIALLY_FULFILLED` حالة حقيقية
  على أوردر اتشحن جزئيًا، ومش شذوذ.

### ③ الفلترة في الواجهة، والـ Worker بيرجّع الحالة خام

الـ Workers بيسألوا شوبيفاي عن **الحالة وبس** (`manual_status` و`status_2_r_e`)
وبيرجّعوا الحقول الخام. مفيش أي شرط تاني في الـ `query`.

- 🔴 **ليه:** الأداة عرض بحت ومالهاش بوابة كتابة تحمي حد من حاجة، وأي فلترة
  زيادة في الاستعلام معناها أوردر **بيختفي في صمت**.
- ⛔ **وممنوع فلتر على ميتافيلد مش قابل للفلترة.** `package_whereabouts_s1`
  مثلاً بيتجاهَل **في صمت** ويرجّع المتجر كله (١٠٬٠٠٠ صف — مقيس 15-09-2026 في
  `package-transfer-to-office`). الحقلين المستخدمين في الفلتر هنا **مؤكَّدين
  قابلين للفلترة**.

## 🔴 الفلوس — «مستحق التحصيل»

```js
dcoCod(rows) → { due, dueCount, prepaidCount, unknownCount, currency }
```

- 🔴 **`currentTotalPriceSet` مش `totalPriceSet`** (`order-lifecycle` قاعدة ٣).
  التاني بيمسك القيمة **الأصلية** وبيفضل زي ما هو بعد أي تعديل أو مرتجع —
  يعني رقم **أكبر** من اللي المندوب هيحصّله فعلاً.
- 🔴 **`PENDING` بالظبط.** الأوردر المدفوع أونلاين مش تحصيل، وجمعه بيخلّي
  «المستحق» أكبر من الحقيقة.
- 🔴 **والاستبعاد مُعلَن مش صامت** — «مدفوع مقدمًا: N» بتتعرض جنب الرقم.
  استبعاد صامت من **رقم فلوس** بيخلّي الفرق بين المجموع والواقع بلا تفسير.
- ⚠️ والصف اللي مالوش حالة مالية بيتعدّ في `unknownCount` — **لا بيتجمع ولا
  بيتستبعد في صمت**.
- 🔴 **وشكل «مستحق التحصيل» مختلف عن مربعات العدّ عن قصد** (`.dco-money` مش
  `.dco-chip`). المربعات اللي جنبه بتعدّ **أوردرات**؛ ده بيقول **جنيه**. نفس
  الشكل كان هيخلّي «٢٬٧٥٠» جنب «١٣» تتقري عدد أوردرات — رقم غلط شكله سليم.
- ⚠️ **وفي الصفحة الرقم بيتحسب على المعروض** (بعد الفلتر) وبيقول **«(مفلتر)»**
  صراحةً. الموظف اللي فلتر على «بوسطة» عايز يعرف بوسطة عليها كام — بس لازم
  يعرف إن الرقم بقى عن المفلتر.

## 🔴 الماكينة — كل صف بياخد بيانات ماكينته هو

الصف ممكن يكون عن **الشحنة الأصلية** (S1) أو **دورة الاستبدال/الاسترجاع** (S2).

```js
dcoMachineOf(o, status) → 's1' | 's2' | null   // S1 الأول
```

- 🔴 **وقت التغليف واسم اللي غلّف ورقم التتبع والعهدة كلهم من حقول الماكينة
  الصح** — `s2_packing_date_time` لصف S2، مش `s1_…`.
  ⚠️ الفخ ده **مقيس حيًا** في `package-transfer-to-office` (15-09-2026): ٦ من
  ٦ صفوف S2 كانت هتعدّي بفحص S1، والصح **٢**. وهنا الأثر مش «طرد ما اتغلّفش
  بيعدّي» — الأثر إن الشاشة بتقول **«متأخر ١٢ يوم»** على طرد اتغلّف امبارح.
- 🔴 **وأوردر حالتَيه على نفس القيمة بيتحسب S1 مرة واحدة وبياخد علامة** — مش
  بيتقسم لصفّين. صفّين لنفس الأوردر معناهم إن الرقم الكبير بيعدّه مرتين.
- ✅ وفيه بندان في فحص المتصفح بيقفلوا ده: وقت تغليف S2 في طابور «جاهز»،
  ورقم تتبع S2 في طابور «مشحون».

## 🔴 رقم التتبع — الجديد أولاً والقديم بعلامة

`custom.bosta_tracking_number` **بيتوقف** ومحلّه `…_s1` و`…_s2` (اتعملوا
13-09-2026). الـ Worker بيقرا **التلاتة**، والواجهة بتعلّم الصف اللي رقمه جه من
الحقل القديم بـ**«من الحقل القديم»**.

> ⚠️ **العلامة دي مش تزويق** — من غيرها الموظف بيفتكر إن الحقول الجديدة
> اتملّت وهي لسه فاضية على كل الأوردرات.

## 🔴 قاعدة الأسبقية — أهم قاعدة في الريبو ده

```html
<!-- في كل صفحة، بالترتيب ده بالظبط -->
<link rel="stylesheet" href="shared/shell.css">
<style>
  /* CSS الصفحة — بيغلب shell.css عند أي تعارض */
</style>
```

**`shell.css` فيه الـ chrome بس. أي CSS خاص بأداة بيفضل في صفحته.**

| ✅ يدخل `shell.css` | ❌ مايدخلش (يفضل في الصفحة) |
|---|---|
| كتلة التوكنز كلها | `.data-table` · `.count-box` · `.cb-*` |
| reset + `body` + `.container` | `.fchip*` · `.srch-*` · `.time-badge` |
| `.app-header*` · `.hbtn` | `.type-text` · `.fin` · `.flag-*` · `.fl-*` |
| `.main-tabs-bar` · `.card` (شِل) | `.q-fail` · `.q-warn` · `.q-empty` |
| `.eco-overlay` · `.eco-modal*` | `.card`/`.card-body` بتوع الصفحة |
| `.settings-*` · `.about-sec*` · `.cl-*` · `.diag-*` | |
| `.toast-container` · `.toast` | |
| `.btn-primary` · `.btn-ghost` · `.btn-green` · `.btn-red` · `.btn-outline` | |
| `.order-link` · `.order-num` | |
| `.dco-home-*` · `.dco-row*` · `.dco-tool*` · `.dco-card-*` · `.dco-num`/`.dco-count` · `.dco-chip*` · `.dco-fresh*` · `.dco-ago*` · `.dco-stamp-chip` · `.dco-rbtn*` · `.dco-fail` · **`.dco-money`** | |

> 🔴 **كتلة التوكنز في `shared/shell.css` بس.** أي كتلة توكنز في صفحة = التوكنز
> اتفرّقت. التلات صفحات الأصلية كلها Tier L فمحتاجتش حتى `--container-max`.
> ⚠️ **والاستثناء بدأ v1.5.0 على `Order-Status-Updater.html` و
> `COD-Payment-Center.html`، وانضمّت لهم `Partial-Delivery.html` في
> v1.10.0** — فيهم **تاب سجل**، يعني Tier M (1200) مش L (1400)، فكل واحدة
> فيها **سطر واحد**: `:root { --container-max: 1200px; }`.
> 🔴 **وسطر واحد ≠ كتلة توكنز** — وفيه بند في `docs/tools-check.mjs` بيقرا
> كل `:root` في التلات صفحات وبيفشل لو فيها **أي متغيّر تاني** غير ده.

> 🔴 **`shared/shell.css` و`shared/shell.js` نسخة من هب المخزن — والخطر ده
> لازم يتقال بالاسم.** درس R1: منطق واحد في ملفين بيفترق مع أول تعديل. والنسخ
> هنا **حتمي** مش اختيار: الريبوهين منفصلين وكل واحد بيتنشر على GitHub Pages
> بتاعه.
> ⚠️ **اللي بيقلل الخطر تلات حاجات:** ① `shell.css` **توكنز وchrome بس** فالفرق
> لو حصل **بصري مش سلوكي**. ② §QUEUE-RULES في `shell.js` **مالهاش أي مقابل**
> في هب المخزن — دي قواعد الأداتين دول وبس، فمفيش نسختين منها أصلاً.
> ③ أي تعديل على الـ chrome يتعمل في **الهبين في نفس التمريرة**.
> ⛔ **والاختلافات المقصودة دلوقتي:** البادئة `dco-` بدل `woc-` · مربعات قناة
> الطباعة اتشالت · `.tool-ship`/`.tool-return` اتشالوا (معانيهم بتاعة محطة
> المخزن) · و**دوال الوقت بقت `Intl`** (تحت).

## 🔴 التوقيت — `Intl` من أول يوم، مش إزاحة ثابتة

هب المخزن لسه شايل `toCairo = +3 ساعات` ثابتة، وهو النمط اللي
`ecommoda-constants` §13 بيسمّيه **كاسر**: مصر بترجع **UTC+2 يوم 29-10-2026**،
وساعتها كل وقت معروض بيغلط بساعة **والأداة مابتشتكيش**.

- ✅ **الهب ده بيبدأ صح**: `cairoParts` بـ`Intl.DateTimeFormat` على
  `Africa/Cairo`، ونفس النسخة بالحرف في الـ Workers.
- 🔴 **وحارس `Invalid Date` إلزامي** — `formatToParts` بترمي `RangeError` على
  تاريخ باظ، وصف واحد بتاريخ غلط كان كفاية **يوقّع رسم الجدول كله**: الطابور
  بيختفي بالكامل عشان صف واحد. `cairoParts` بترجّع `null` والمستدعي بيعرض `—`.
- ⚠️ **والفرق ده معناه إن الهب ده مش في نطاق بند 29-10-2026 أصلاً** — لا في
  الواجهة ولا في الـ Workers.

## عقد الجلسة — `sessionStorage`

```js
const DCO_SESSION_KEY   = 'dco_session';        // الهوية
const DCO_CACHE_READY   = 'dco_queue_ready';    // كاش طابور الجاهز
const DCO_CACHE_SHIPPED = 'dco_queue_shipped';  // كاش طابور المشحون
const DCO_AUDIT_READY   = 'dco_audit_ready';    // 🔴 حالة جرد المكتب (v1.3.0)
const DCO_CACHE_TTL_MS  = 15 * 60 * 1000;
```

```js
{ v: 1, username: 'Ahmed_Ibraheem', displayName: 'Ahmed Ibraheem', loginAt: '…Z' }
```

- **`v` نسخة العقد** — لو الشكل اتغيّر، القديم بيترفض بدل ما يتقري غلط.
- **كل قراءة/كتابة جوّه `try/catch`** — المتصفح في وضع خاص بيرمي على **مجرد
  الوصول**، والرمي ده كان هيمنع فتح الصفحة أصلاً.
- **مفيش انتهاء صلاحية بالوقت** — الجلسة بتموت بقفل التاب أو بالخروج بس.

> 🔴 **`dco_session` مش `woc_session` — والفرق مقصود وأهم مما يبدو.** الهبين على
> **نفس الـ origin** (`ecommoda-dev.github.io`)، يعني `sessionStorage` مشترك
> بينهم حرفيًا. مفتاح موحّد كان معناه إن الموظف اللي دخل محطة المخزن يلاقي نفسه
> **داخل هنا من غير ما يدخل** — والنتيجة إن **صف الدخول بتاع المركز ده مايتكتبش
> خالص**، فسؤال «مين فتح مركز الشحن النهاردة؟» يفضل بلا إجابة.
> ⚠️ **الثمن:** الموظف بيدخل مرتين لو فتح الاتنين. ده **مقبول ومقصود** — دي
> محطتان مختلفتان، والدخول هو الأثر الوحيد اللي بيقول مين كان فين.

### كاش الطوابير

```js
{ at: '2026-09-15T07:12:44.101Z', data: <رد الـ endpoint كامل> }
```

- بيتكتب بعد **كل جلب ناجح** من أي صفحة.
- الصفحتان بيقروا كاشهم **عند الفتح** فالشاشة مش بتفضل فاضية، والرسم من الكاش
  **مختوم بوقته**.
- 🔴 **والكاش بيتخزّن خام** — الصفحة بتشتق منه بنفس دوال الـ shell. كاش
  **مشتق** كان هيخلّي أي تعديل في القاعدة يسيب بيانات قديمة **بشكل قديم**
  في الجلسة.
- `QuotaExceededError` بيتبلع بصمت — الكاش **تحسين مش شرط**.

### `requireSession()` — أول سطر في كل صفحة محمية

> 🔴 **بترمي عن قصد.** من غير الرمي، باقي سكربت الصفحة بيكمّل تنفيذ وبينادي
> الـ Worker وبيرسم جدول **أثناء** ما التحويل شغّال — نداءات ضايعة ووميض شاشة.

> ⚠️ **`location.replace` مش `location.href`** — عشان صفحة محمية مايتسجّلش لها
> مدخل في تاريخ المتصفح.

> 🔴 **`next` بيتفحص بـ regex قبل التحويل** (`NEXT_RE`). من غير الفحص
> `?next=https://evil…` بيحوّل الموظف لبرّه الهب. وفيه بند في فحص المتصفح
> بيقفل ده.

## الشاشة الرئيسية — القواعد

| # | القاعدة | ليه |
|---|---|---|
| ١ | التحديث التلقائي كل ١٥ دقيقة **بحراسه التلاتة** | من غيرهم ده polling أعمى على أتقل endpoint × عدد الأجهزة × طول اليوم |
| ٢ | الطابورين بيتجابوا **بالتوازي** بـ`allSettled` | Workers مختلفين = isolates مختلفين. `allSettled` مش `all` — فشل طابور مايخفيش التاني |
| ٣ | **العدد قبل أول جلب ناجح = `—` مش `0`** | «ما اتحدّثش» ≠ «مفيش شغل» |
| ٤ | **الفشل بيعرض حالة فشل مش صفر** | «تعذّر الاستعلام» ≠ «مفيش شغل». والدخول للصفحة **لسه شغّال** |
| ٥ | **ختم الوقت بيتسجّل حتى لو الطابور رجع فاضي**، وفي الفشل **مايتغيّرش** | «اتحدّث ورجع صفر» معلومة. وما نقولش للموظف إن القايمة جديدة وهي مش جديدة |
| ٦ | الضغط على الصف بيفتح الصفحة **حتى والجلب فشل** | الطابور مش شرط لقراءة الصفحة |

### الحراس التلاتة

| # | الحارس | بيمنع إيه |
|---|---|---|
| ① | التسليح بعد **كل جلب ناجح** (`armAuto` جوّه `loadQueue`) | «١٥ دقيقة من آخر تحديث فعلي» مش من الدخول |
| ② | `document.hidden` = **مفيش استعلام** — بيتعلّم `dirty` والجلب على `visibilitychange` | استعلام على شاشة محدش شايفها |
| ③ | النبضة (٣٠ ثانية) بتحدّث «منذ كام» **بلا أي نداء شبكة** | إعادة رسم الجدول كانت هتصفّر التمرير تحت إيد الموظف |

> ⚠️ **تايمر لكل طابور لوحده** (`homeQ[key].timer`) مش تايمر واحد للاتنين —
> Workers مستقلين وواحد ممكن يقع والتاني ينجح (قاعدة ٢). تايمر موحّد كان
> هيربط تسليح الناجح بفشل التاني.

> 🔴 **قايمة الجلب بتتبني من `Object.keys(homeQ)` — مش مكتوبة بالإيد.** القايمة
> اليدوية هي اللي بتنسى طابور جديد **في صمت**: الصف بيترسم، والعدّاد بيفضل `—`
> **للأبد**، ومفيش أي خطأ في الكونسول. ده حصل فعلاً في هب المخزن وقت إضافة
> الطابور الرابع، **واتمسك في فحص المتصفح مش في مراجعة كود**.

> ⚠️ **زرار «تحديث» جوّه كل صف لوحده — ممنوع يتوحّد.** زرار واحد للاتنين بيخبّي
> مين اللي فشل، وده بالظبط اللي قاعدة ٤ اتكتبت ضده.

> ⚠️ **الفشل بعد نجاح سابق: الرقم القديم بيفضل معروض** (مش «تعذّر») — والعدّاد
> الأحمر فوقه هو اللي بيقول إنه قديم. «تعذّر» بتتعرض **بس** لو مافيش أي جلب
> ناجح قبل كده (`homeQ[key].at === null`).

**سلّم لون «آخر تحديث»** (`agoInfo`):

| المدة | اللون | الزرار |
|---|---|---|
| < ٥ دقايق | أخضر | أزرق |
| < ١٥ دقيقة | أزرق | أزرق |
| < ٦٠ دقيقة | كهرماني | **كهرماني** (`is-stale`) |
| ≥ ساعة | أحمر «الرقم قديم، حدّث» | **كهرماني** |

## فخاخ الصفحتين

- 🔴 **الرقم الكبير = عدد الطابور، مش عدد المعروض.** الفلتر أو البحث
  **مابيغيّروش** — واللي بيتغيّر **«النتائج: N»** في صف الجدول.
  ⚠️ رقم كبير بيتغيّر مع الفلتر بيخلّي الموظف يفتكر إن الطابور قلّ.
  ⚠️ وسطر «معروض N من M» **اتشال في v1.2.0** — بقى تكرار لنفس المعلومة في
  مكان تالت بعد ما «النتائج» دخلت.
- 🔴 **مربعات الفلترة بتعدّ من القايمة الكاملة مش المفلترة.** العدّ على المفلتر
  كان هيصفّر باقي المربعات بعد أول ضغطة، **فالموظف مايقدرش يرجع منها**.
  ⚠️ **وبقوا صفّين من v1.8.0** — المندوب فوق، و**موقع الشحنة** تحته (صفحة
  الجاهز بس). الاتنين بيكتبوا في فلاترهم في اللوحة تحت، والتفاصيل في قسم
  «تمريرة v1.8.0».
- 🔴 **الفلتر بقى متعدد من v1.2.0** — القاعدة القديمة («أحادي: مربع واحد
  مختار على الأكثر») **اتلغت**: الموظف اللي عايز «بوسطة + مناديب» كان مضطر
  يشيل الفلتر خالص، والمعيار بيقول **كل الفلاتر اختيار متعدد** بلا استثناء.
  ⚠️ واللي بيمنع الالتباس اللي القاعدة القديمة اتكتبت ضده هو **صف الـ chips**:
  المختار بيتكتب بالاسم تحت الفلاتر، فالموظف شايف هو فلتر على إيه بالظبط.
- ⚠️ **«مفيش نتيجة للفلتر» ≠ «الطابور فاضي»** — رسالتين مختلفتين، والخلط بينهم
  بيخلّي الموظف يفتكر إن الشغل خلص.
- 🔴 **بانر الفشل وبانر الاقتطاع منفصلين** — «الطابور ما اتجابش» حاجة،
  و«الطابور أطول من اللي بيتعرض» حاجة تانية خالص.
- 🔴 **زرار العلامة بيتربط بـ`addEventListener` مش `onclick` بنص**، والصف
  بيتربط **بفهرس المعروض**. اسم العميل وسبب العلامة بيتحطّوا في نص HTML،
  و`onclick="showFlags('...')"` كان بيسرّب أي علامة تنصيص فيهم.
- ⚠️ **النبضة بتحدّث البادجات في مكانها** بالـ ISO المخزّن على الخلية نفسها
  (`data-q-age` · `data-q-pack`) — **مش بترتيب الصفوف**، لأن الترتيب بيتغيّر مع
  الفلتر.
- ⚠️ **من غير `placeholder` في مربع البحث** (Step 2 بند ٧) — النص الرمادي جوّه
  المربع بيتقري **قيمة مكتوبة** من بعيد. و`aria-label` هو الوصف الباقي لقارئ
  الشاشة.

### 🔴 §TABLE — القسم الموحّد (v1.2.0 · طلب أحمد 16-09-2026)

🔴 **المصدر: `ecommoda-html-builder` → `references/data-table-standard.md`،
والتنفيذ نسخة من `pack.html` بالحرف.**

> 🔴 **والبند ده اتكتب بعد تقصير حقيقي في v1.1.0 — ولازم يتقال:** التمريرة
> اللي قبلها نقلت **شكل** الجدول (الحشو · الخطوط الرأسية · هيدر الـ2px) وسابت
> **بنيته**: لا لوحة فلاتر ولا ترتيب ولا صف نتايج. المعيار موجود في المهارة
> **ومكتوب فيه إن أي جدول بيتفتح للتعديل يتحوّل له بالكامل** — والقراءة
> الضيقة لكلمة «جدول» خلّت التمريرة تعدّي على البند ده كله.
> ⛔ **القاعدة من دلوقتي:** أي تعديل على جدول في الريبو ده بيتقاس على
> `data-table-standard.md` **كله**، مش على اللي اتطلب حرفيًا.

| الجزء | اللي فيه |
|---|---|
| **كارت واحد** (`.unified-section`) | الفلاتر والجدول **جسم واحد** بينهم `.section-divider`. ⛔ ممنوع كارتين بينهم فراغ · ⛔ وممنوع `overflow:hidden` عليه (بيقص قوايم الفلاتر) |
| **هيدر الفلاتر** | سهم الطي **أول عنصر أقصى اليمين** · أيقونة قمع **من غير كلمة «الفلاتر»** · «مسح كل الفلاتر» أقصى الشمال |
| **صف ①** | خمس فلاتر **اختيار متعدد** (v1.7.0): المندوب · نوع الأوردر · **موقع الشحنة** (الجاهز بس) · **التغليف** · المراجعة |
| **صف ②** | بحث ← فترة سريعة ← من ← إلى (**الترتيب إلزامي**) |
| **صف ③+** | chips — **سطر لكل فلتر**، واسم الفلتر مرة واحدة في أوله |
| **صف الجدول** | سهم الطي · **«النتائج: N»** · زرار تحديث بـ`stopPropagation` |
| **الجدول** | كل الأعمدة `sortable-th` بدورة تلات حالات |

- 🔴 **`qAnyFilterActive` بتستخدم `!!value` مش `!== null`** — ده **أخطر باج في
  المعيار** (§3): `undefined !== null` بترجّع `true`، فأي خاصية مش موجودة على
  فلتر كانت هتخلّي أيقونة الفلتر **مولّعة من أول تحميل وللأبد**. اتكتب غلط
  تلات مرات متتالية في الستاك.
  ✅ **والتلات حالات متقفولة ببنود في فحص المتصفح**: تحميل → رمادي وباهت ·
  اختيار → `--accent` وأحمر · «مسح كل الفلاتر» → رجوع للحالة الأولى بالظبط.
- 🔴 **حارس `document.contains(e.target)` إلزامي** في listener قفل القوايم
  (`multi-select-filter.md` §8). من غيره القايمة بتتقفل بعد **أول** اختيار:
  `qMsRenderList` بتشيل العنصر المضغوط من الـ DOM قبل ما الحدث يوصل للـ
  listener، و`wrap.contains(detached)` بترجّع `false` فالكود بيفتكر الضغطة
  برّه. **والفلتر يبقى multi-select بالاسم بس** — بلا أي رسالة خطأ.
- 🔴 **مربعات الطابور بقت بتكتب في الفلاتر — مش حالة تانية جنبها.** حالتان
  منفصلتان (مربع أحادي فوق + فلتر متعدد تحت) كانوا هيفترقوا: الموظف يفلتر من
  تحت والمربع فوق يفضل مطفي، فيدوس عليه فيدهس فلتره **من غير ما يقصد**.
  ⚠️ **والفلتر بقى متعدد** — القاعدة القديمة («الفلتر أحادي») **اتلغت**:
  الموظف اللي عايز «بوسطة + مناديب» كان مضطر يشيل الفلتر خالص.
- 🔴 **الترتيب مستقل تمامًا عن الفلاتر** (§8) — بيشتغل مع الفلتر ومن غيره،
  **وبيفضل شغّال بعد «مسح كل الفلاتر»**. وفيه بند في فحص المتصفح بيقفل ده.
  ⚠️ **الضغطة التالتة بترجّع للافتراضي (الأقدم فوق)** مش لترتيب الـ Worker.
  ⚠️ **و`[...rows].sort()` مش `rows.sort()`** — التانية بتعدّل المصفوفة
  الأصلية، فحالة «بلا ترتيب» ماكانتش هترجع أبدًا.
  ⚠️ **والصف اللي مالوش تاريخ بيروح الآخر في الاتجاهين** — `-Infinity` كان
  بيحطّه **فوق** في التصاعدي وكأنه أقدم صف في الطابور.
- 🔴 **السهم بيظهر بس على العمود المرتَّب عليه** — مفيش `⇅` ثابتة على كل عمود.
- 🔴 **فلتر التاريخ بيقارن على يوم القاهرة** (`cairoDayStr`) مش على أول ١٠
  حروف من الـ ISO. `createdAt` بتوقيت UTC ويوم القاهرة بيبدأ `D-1T21:00Z` —
  فالمقارنة النصّية كانت هتحطّ أوردر الساعة ١١ بالليل **في اليوم اللي بعده**.
  ⚠️ **وبلا فترة افتراضية** (§5): الحالة المحايدة «بلا فلتر تاريخ» مش «اليوم»،
  و«✕ مسح الاختيار» إلزامي في **أعلى** قايمة الفترة.
- 🔴 **بنود الفلاتر بتتبني من الطابور الكامل بعد كل جلب** (`qSyncMsItems`) —
  بناؤها من المفلتر كان هيخلّي أول اختيار **يشيل باقي الخيارات من القايمة**،
  وبناؤها مرة واحدة عند التحميل كان هيخلّي مندوب جديد يدخل الطابور
  **ومايظهرش في الفلتر أبدًا** بلا أي خطأ.
  ⚠️ والاختيار القديم بيتشال لو قيمته مابقتش موجودة — فلتر على قيمة مختفية
  معناه جدول فاضي بلا سبب ظاهر.
- 🔴 **«النتائج: N» = المعروض بعد الفلتر · والرقم الكبير = الطابور كله.**
  الاتنين على الشاشة عن قصد، والفرق بينهم هو أثر الفلتر. وسطر «معروض N من M»
  القديم **اتشال** — بقى تكرار لنفس المعلومة في مكان تالت.
  ⚠️ وقبل أول جلب ناجح «النتائج» بتقول `—` مش `0` — زي الرقم الكبير بالظبط.
- ⚠️ **لوحة الفلاتر مقفولة افتراضيًا** — انحراف عن «مفتوح افتراضيًا» في §2،
  **ونفس انحراف `pack.html` و`print.html`**: الطابور هو الشغل، والفلاتر كانت
  هتاخد نص الشاشة الأولى قبل أول صف. و`qFltOpen = false` في الـ JS لازم يفضل
  متطابق مع `class="flt-body collapsed"` في الـ HTML، وإلا أول ضغطة على
  الهيدر مابتعملش حاجة ظاهرة.
- ⚠️ **🔍 على الشمال إلزاميًا** + `padding-left` على المربع — على اليمين
  بتعمل overlap مع الكتابة العربية.
- 🔴 **`data-q-sort` في الـ HTML لازم يطابق مفاتيح `Q_SORT_CONFIG` بالحرف** —
  مفتاح مش في القايمة بيرجع للافتراضي **في صمت**، فالعمود يبان إنه اترتّب
  وهو مااترتّبش.

### 🧾 تمريرة الأعمدة والشكل (v1.1.0 · طلب أحمد 16-09-2026)

**صفر تعديل في `shared/shell.js` §QUEUE-RULES · صفر تغيير في أي رقم** — كلها
عرض، ماعدا عمود العنوان اللي محتاج `ready-orders-worker` v1.1.0.

| # | التغيير | السبب |
|---|---|---|
| ① | **«طابور الجاهز للشحن» → «قسم الجاهز للشحن»** | يطابق تسمية محطة المخزن («قسم التغليف» · «قسم الطباعة» · «قسم تسليمات بوسطة») والموظف بيتنقّل بين الهبين |
| ② | **مربعات الفلترة بقت مستطيلة** (`.zchip` بدل `.fchip`) | نسخة من `pack.html` بالحرف — مربع فلتر بشكلين لنفس الفعل = الموظف يتعلّمه مرتين |
| ③ | **عنوان وشرح صندوق العدّ اتشالوا** | الهيدر بيقول الاسم، والشرح كان بيوصف **قاعدة الاستعلام** — بتتقرا مرة وبتاخد مساحة كل يوم |
| ④ | **الجدول بشكل جدول التغليف بالحرف** | نفس الحشو والخطوط الرأسية وهيدر الـ2px، وبإطار خارجي |
| ⑤ | **عمود «العنوان»** بعد العميل | الموظف بيرتّب الدفعة بالمنطقة، وكان لازم يفتح كل أوردر على شوبيفاي |
| ⑥ | **«عهدة الطرد» → «موقع الشحنة»** | نفس الحقل بالحرف — «عهدة» كلمة محاسبية |
| ⑦ | **«القناة» · «عدد القطع» · «الإجمالي» اتشالوا** | الطابور ده **عرض بحت** ومحطة الشحن مابتتصرّفش عليهم |
| ⑧ | **«نوع الأوردر» بقى قبل الأخير** | أعمدة التعريف (رقم · عميل · عنوان · مندوب) اتجمّعت في الأول |

- 🔴 **الفلوس ما ضاعتش مع عمود «الإجمالي».** «مستحق التحصيل» فوق الطابور لسه
  بيتحسب من `dcoCod` على **نفس** `o.total`/`o.financial` بالحرف، وبيقول
  المدفوع مقدمًا والحالة المالية المجهولة **بعددهم**. اللي اتشال هو **العمود**.
  ⛔ و`financeCell` **اتشالت** معاه — دالة بلا مستهلك = كود ميت.
- 🔴 **والقناة ما ضاعتش من الحكم.** `zone_missing` و`zone_unknown` **لسه
  علامتين شغّالتين** في `dcoFlags`، والقيمة الغلط بتتعرض **بالحرف** في نافذة
  المراجعة. اللي اتشال هو **عمود العرض** — الشذوذ لسه بيتمسك ولسه بيتعدّ.
- 🔴 **وسطر المجموعة تحت اسم المندوب اتشال — مش المجموعة نفسها.**
  `courierGroup` لسه بيتحسب في الـ shell، والمربعات بتعدّ منه والفلتر بيشتغل
  بيه. اللي اتشال هو **تكراره تحت كل اسم** بعد ما بقى معروض بعدده فوق الطابور.
- ⚠️ **والمحافظة اتنقلت مش اتشالت** — كانت سطر تحت اسم العميل وبقت في خلية
  العنوان. نفس القيمة في خانتين على نفس الصف بتخلّي الموظف يفتكر إنهم حاجتين.
- 🔴 **«بلا عنوان» بتتقال بالنص** — خانة فاضية بتتقري «الشاشة بايظة»، والحقيقة
  إن الأوردر ده **فعلاً مالوش عنوان على شوبيفاي** وده شغل محتاج تدخّل.
  ⚠️ وهي **مش علامة مراجعة** — إضافتها لقايمة العلامات (قاعدة ١٣) قرار منفصل.
- ⚠️ **وخلية العنوان هي الوحيدة اللي بتلفّ** (`.addr-cell`) — باقي الجدول سطر
  واحد متوسّط. `max-width` عليها هي اللي بتمنعها تاكل عرض الجدول وتزقّ باقي
  الأعمدة برّه الشاشة، و`text-align:right` عشان العين تمسك أول كل سطر في مكان
  واحد بدل ما تدوّر عليه.

## 🔴 تمريرة v1.9.0 — أرضية تاريخ الطابور وعمود «العميل» (طلب أحمد 17-09-2026)

| # | التغيير | ليه |
|---|---|---|
| ① | 🔴 **الطابورين بيبدأوا من أوردرات `01/04/2026`** — شرط `created_at` في استعلام الـ Worker جنب شرط الحالة | أوردرات دورة قديمة عالقة كانت بتملا الطابور بمئات الصفوف بادجها «متأخر ١٧١ يوم» وبتدفن شغل اليوم |
| ② | 🔴 **عمود «العميل» بقى سطرين بالكتير** — بنط أصغر وحشو أضيق وقص بـ`…`، **بلا أي توسيع للعمود** | الاسم الخماسي كان بيلفّ على **ستة سطور** والصف يبقى ضعف ارتفاع جاره |

### 🔴 ① الأرضية — التغيير في الـ Workers مش في الواجهة

- 🔴 **الشرط في `index.js` بتاع الطابورين** (`ready-orders-worker` `1.4.0` ·
  `shipped-orders-worker` `1.1.0`)، ومكانه هناك مش هنا: الواجهة بتفلتر
  للعرض، والأرضية بتشيل الصفوف **من المصدر** فالرقم الكبير و«مستحق
  التحصيل» والجرد كلهم بيتحسبوا على نفس النطاق تلقائيًا.
- 🔴 **والاستبعاد مُعلَن مش صامت** — شيب **«الطابور من 01/04/2026»** جنب
  الرقم الكبير (`.cb-scope` · `#qScope`)، **وقيمته من رد الـ Worker**
  (`minCreatedDay`) مش مكتوبة في الصفحة.
  ⛔ **والفرق ده هو البند نفسه**: تاريخ مكتوب في الصفحة كان هيفضل ظاهر
  على Worker مش مطبّق فيه الأرضية (درس R1 — نسختان بتتعدّلا بالإيد).
  ⚠️ **وبيختفي خالص لو الرد مافيهوش الحقل** — ادعاء أرضية مش مطبّقة أسوأ
  من مفيش ادعاء. وفيه بند بيقفل الحالتين.
- ⚠️ **وشكله مختلف عن مربعات العدّ عن قصد** (`.cb-scope`) — المربعات
  حواليه بتعدّ **أوردرات**، وده بيقول **نطاق**؛ شكل واحد كان هيخلّي
  «01/04/2026» تتقري رقم تالت في صف أرقام.
- 🔴 **والقاعدة القديمة ما اتكسرتش** — «الفلترة في الواجهة والـ Worker
  بيرجّع الحالة خام» (قرار ③) اتكتبت ضد فلتر **تشغيلي** (زون · مندوب ·
  تغليف) يخلّي أوردر شغّال **يختفي في صمت**. الأرضية نوع تاني: قرار أحمد
  إن اللي قبلها **مش شغل المحطة** أصلاً — وهي معلنة في تلات أماكن (الرد ·
  `diag` · الشاشة).
- ⚠️ **واليوم بتوقيت القاهرة بيتحوّل للحظة UTC بـ`Intl`** جوّه الـ Workers
  (`cairoDayStartUtcISO` → `2026-03-31T22:00:00Z`) — التاريخ المجرّد
  تفسيره عند شوبيفاي مش مضمون، ولو اتفسّر UTC كانت أول ساعات يوم `01/04`
  بتوقيت القاهرة **هتقع بره الأرضية**.
- ⛔ **وتغيير القيمة تغيير في المعنى** — لازم ينزل على **الطابورين** وعلى
  نص «عن الأداة» في نفس التمريرة.

### 🔴 ② عمود «العميل» — تصغير وقص، مش توسيع

```css
.data-table td.cust-cell { padding-inline: 6px; }
.cust-txt { display: -webkit-box; -webkit-line-clamp: 2; overflow: hidden;
            font-size: 12.5px; line-height: 1.4; overflow-wrap: anywhere; }
```

- 🔴 **التوسيع كان مرفوضًا صراحةً** — والعرض الزيادة مالوش مصدر غير
  «العنوان» و«ملحوظات»، وهما **العمودان اللي بيتقروا فعلاً** (تمريرة
  v1.7.0 اتعملت عشان تدّيهم العرض ده بالظبط).
- 🔴 **والتلاتة لازمين مع بعض** (زي خلية التاريخ في v1.8.0): حشو أضيق
  (٧٨px محتوى بدل ٦٨) + بنط أصغر بدرجتين → **كلمتين في السطر بدل كلمة**،
  وقص عند سطرين. التصغير لوحده **بيقلّل** السطور مش **بيسقّفها**.
- ⛔ **والقص على `<span>` جوّاني مش على `<td>`** — `display:-webkit-box`
  على خلية جدول بيشيلها من تخطيط الجدول **والصف بيتفكك**.
- 🔴 **والاسم الكامل مش ضايع** — في `title` على الخلية · وفي **البحث**
  (بيدوّر في الاسم الكامل مش في المعروض) · وفي **تصدير الجرد**. قص بلا
  منفذ للقيمة الكاملة كان هيخلّي الموظف يفتح الأوردر على شوبيفاي عشان
  يقرا اسم.
- ⚠️ **والدالة واحدة للجدولين** (`customerCell`) — جدول الطابور وجداول
  تاب الجرد، ونسختان كانوا هيخلّوا الصف يتقري بشكلين في تابين من نفس
  الصفحة.
- ✅ **وتمن بنود في `docs/queues-check.mjs`** بيقروا **التخطيط الفعلي**:
  عدد السطور المرسومة ≤ ٢ · والبنط أصغر من بنط الجدول · والاسم **جوّه
  العمود** · و`title` فيه الاسم كامل · و**العرض ما اتغيّرش** · والبحث
  بيلاقيه بآخر الاسم · وفي **الصفحتين**.

## 🔴 تمريرة v1.8.0 — مربعات «موقع الشحنة» وسطرا التاريخ (طلب أحمد 17-09-2026)

**صفر تعديل في `shared/shell.js` §QUEUE-RULES و§AUDIT-RULES · صفر تغيير في
أي رقم بيتعرض** — كلها عرض وفلترة. والـ Workers ما اتلمسوش.

| # | التغيير | ليه |
|---|---|---|
| ① | **خلية التاريخ بقت سطرين بالظبط** في العمودين — سطر للتاريخ **بأيقونته معاه**، وسطر للبادج | الأيقونة 📅 كانت بتنزل **لوحدها في سطر**، والخلية تبقى تلات سطور بسطر أول فيه **رمز بلا قيمة** |
| ② | **بند الفاضي في قايمة فلتر «موقع الشحنة» بقى `—`** بدل «— مش مسجّل» | البند في القايمة كان بيتقري **قيمة تانية** غير اللي في الخلية، والموظف بيدوّر على `—` ومايلقهوش |
| ③ | **صف مربعات «موقع الشحنة»** تحت مربعات المندوب في صندوق العدّ | «وَرّيني اللي مش في المكتب» سؤال يومي، وإجابته كانت **فتح لوحة الفلاتر** واختيار من قايمة |

### 🔴 ① سطرا التاريخ — التلاتة لازمين مع بعض

```css
.data-table td.date-cell { padding-inline: 6px; }                    /* ② */
.date-cell .cell-date { display: block; white-space: nowrap;         /* ① */
                        font-size: 13.5px; margin-bottom: 4px; }     /* ③ */
```

- 🔴 **الرقم اللي بيفسّر كل حاجة:** `📅 14/09/2026` عرضه **٩٥px** على بنط
  الجدول (14.5px)، والعمود 112px بيسيب **٩٠px** جوّه بعد الحشو (11px × ٢).
  الفرق **٥px** بس — وهو اللي كان بينزّل الأيقونة سطر لوحدها.
- ⛔ **و`white-space:nowrap` لوحده كان بيكسر تمريرة v1.7.0**: المحتوى اللي
  مش بيلفّ بيرفع **أقل عرض** للعمود، فالمتصفح بيوسّع عمودَي التاريخ فوق
  الـ112 **بعرض محتواهم** — وبادج «متأخر ١٦ يوم» أطول من «منذ ٣ أيام**»**،
  فالعمودان **يفترقوا تاني**. فالحشو الأضيق والبنط الأصغر مش تزويق: هما
  اللي بيخلّوا المحتوى يدخل فعلاً.
- ⚠️ **والفسحة بقت ١٢px مش ٥** (١٠٠px جوّه · ٨٨px محتوى) — رمز 📅 بيترسم
  من **خط النظام** (Noto Color Emoji · Segoe UI Emoji على ويندوز) وعرضه
  بيفرق ٢-٤px من جهاز للتاني. فسحة ٥px كانت هتخلّي السطر الواحد مضمون
  **على جهاز المطوّر بس**.
- ⚠️ **والأيقونة فضلت مكانها** — شيلها كان أسهل حل وأسوأ حل: الرمز هو اللي
  بيفرّق عمود تاريخ عن عمود رقم في لمحة.
- ✅ **وفيه أربع بنود × صفحتين في `docs/queues-check.mjs` بيقروا التخطيط
  الفعلي** (`getClientRects().length === 1`) مش قيمة CSS — لفّ السطر
  بيحصل من الخط والعرض الفعليين، ومقارنة على `white-space` كانت هتعدّي
  على الحالة اللي بتكسر.

### 🔴 ② الفاضي `—` — قيمة واحدة في تلات أماكن

```js
const WA_NONE = '—';     // القيمة الخام **و**الليبل مع بعض
```

- 🔴 **الخلية والقايمة والمربع بيقولوا نفس النص بالحرف.** «— مش مسجّل»
  كانت جملة **صح في معناها** وغلط في مكانها: القايمة بتتقري بند تاني غير
  اللي في الجدول.
- ⚠️ **والمعنى ما ضاعش** — «`—` معناها محدش سجّل مش المخزن» مكتوبة في
  **«عن الأداة»**، ⛔ ومش مسموح تتشال من هناك: ده المكان الوحيد الباقي
  اللي بيقول الفرق ده بالنص.

### 🔴 ③ مربعات «موقع الشحنة» — صف تاني في صندوق العدّ

| المربع | اللون | مصدره |
|---|---|---|
| ✅ في المكتب | أخضر | `waIsOk(v)` — **نفس** شرط البادج في العمود |
| ⚠ المخزن · ⚠ مع المندوب | أحمر | أي قيمة تانية |
| `—` | رمادي محايد | ⛔ **مش أحمر** — «محدش سجّل» مش «مكان غلط» |

- 🔴 **العدّ من `of` بتاعة الفلتر نفسها بالحرف** (`Q_FILTERS` → `where`) —
  مش من `o.whereabouts` مباشرةً. قاعدتان (واحدة للعدّ وواحدة للفلترة)
  بيفترقوا مع أول قيمة غريبة: مربع بيقول «٦٤» بيفتح جدول بـ٦٣ صف **بلا
  أي خطأ**. ✅ وفيه بند بيضغط كل مربع ويقارن رقمه بعدد الصفوف اللي فتحها.
- 🔴 **والمربع بيكتب في الفلتر — مش حالة تانية جنبه** (نفس قاعدة مربعات
  المندوب بالحرف): حالتان منفصلتان كانوا هيفترقوا، فالموظف يفلتر من
  اللوحة تحت والمربع فوق يفضل مطفي، فيدوس عليه **فيدهس فلتره**.
  ✅ وفيه بند في **الاتجاهين**: ضغطة المربع بتولّع الفلتر، واختيار من
  القايمة بيولّع المربع.
- 🔴 **والترتيب ثابت والأربعة بيظهروا حتى بصفر** — الترتيب بالعدد بيرقّص
  المربعات مكانها مع كل تحديث والموظف بيدوس على **مكان** مش على كلمة،
  ومربع بيختفي معناه إن «مفيش ولا طرد مع المندوب» بقت **غياب** بدل ما
  تكون معلومة. ✅ والبند بيقيسها على طابور **ناقصة منه القيمة دي فعلاً**.
- 🔴 **وأي قيمة بره القايمة بتاخد مربعها** — من غير كده الصف ده مايكونش
  ليه مربع، **ومجموع المربعات يقلّ عن الرقم الكبير في صمت**. ✅ وفيه بند
  بيجمع المربعات ويقارن بالرقم الكبير.
- ⚠️ **والعدّ من الطابور الكامل مش من المعروض** — زي مربعات المندوب
  بالظبط: العدّ على المفلتر كان هيصفّر باقي المربعات بعد أول ضغطة.
- ⚠️ **وتحت صف المندوب مش جنبه** — صفّان في سطر واحد بيتقروا **مجموعة
  واحدة**، والموظف يفتكر إن ضغطتين منهم بيضيّقوا نفس السؤال.
- ⛔ **وصفحة المشحون مالهاش الصف ده** — مفيش فيها فلتر «موقع الشحنة»
  أصلاً (العمود التاني هناك رقم تتبع)، ومربع بيفلتر على فلتر مش موجود =
  ضغطة مالهاش أثر. وفيه بند بيمنع إضافته «بالقياس».

## 🔴 تمريرة v1.7.0 — الفلاتر والمقاسات وتاب الجرد (طلب أحمد 17-09-2026)

**صفر تعديل في `shared/shell.js` §QUEUE-RULES و§AUDIT-RULES · صفر تغيير في
أي رقم بيتعرض** — كلها فلاتر وعرض. والـ Workers ما اتلمسوش.

| # | التغيير | ليه |
|---|---|---|
| ① | **قايمة فلتر «موقع الشحنة» بقت بنص البادج بالحرف** — «✅ في المكتب» · «⚠ المخزن» · «⚠ مع المندوب» بدل `Office`/`Warehouse`/`Courier` | الموظف كان بيشوف كلمة في الخلية وبيدوّر عليها في القايمة **ومايلقهاش**، فيفتكر إن القيمة دي مش قابلة للفلترة أصلاً |
| ② | **فلتر «التغليف»** — «تم التغليف» · «لم يتم التغليف» | السؤال ده إجابته الوحيدة كانت **ترتيب** العمود وقراءة الصفوف بالعين |
| ③ | **مقاسات الأعمدة** — «العميل» −٣٥٪ · عمودا التاريخ **بنفس العرض بالظبط** · والباقي لـ«العنوان» و«ملحوظات» | التخطيط التلقائي كان بيوزّع الفراغ بالتناسب مع المحتوى، فالعمودان اللي بيتقروا فعلاً كانوا أضيق اتنين نسبةً لمحتواهم |
| ④ | **تاب «الطابور» بقى «الأوردرات»** | يطابق اسم الكارت في الشاشة الرئيسية («أوردرات جاهزة للشحن») — الموظف بيدوس على الكارت ولازم يلاقي نفس الكلمة |
| ⑤ | **تاب «جرد المكتب» بقى له فلاتره هو** (المندوب كمجموعة · موقع الشحنة) | الشرح الكامل في قسم «النطاق» تحت |

### 🔴 `labelOf` — الليبل غير القيمة

```js
{ key: 'where', label: 'موقع الشحنة', of: o => o.whereabouts || WA_NONE, labelOf: waFilterLabel }
```

- 🔴 **الفلترة كلها لسه على `of(o)` الخام** (`Office`)، و`labelOf` بتغيّر
  **العرض بس** — في القايمة وفي الشيب وعلى زرار الفلتر.
  ⛔ **وممنوع الفلترة تتحوّل للنص المعروض** — ده بيربط الفلتر بشكل البادج،
  وأي تعديل في نص البادج بيفضّي الجدول **في صمت**.
- 🔴 **وخريطة واحدة للعمود وللفلتر** (`WA_MAP` · `waText` · `waFilterLabel`
  في `WA_FN`) — نسختان من نفس التسمية بيفترقوا مع أول قيمة جديدة (درس R1).
- ⚠️ **وترتيب بنود القايمة بقى بالليبل** مش بالقيمة الخام — القايمة بتتقرا
  بالليبل، وترتيب `Office`/`Warehouse` تحت أسماء عربية بيبان عشوائي.

### 🔴 §MULTISELECT — مكوّن واحد بنطاقات

قوايم الاختيار المتعدد بقت **نسخة واحدة** بتاخد **نطاق** (`ns`) وبتولّد منه
مفاتيح الـ DOM (`${ns}MsBtn-${key}`): نطاق `q` للطابور ونطاق `aud` للجرد.

- 🔴 **السبب مش تنظيمي** — تاب الجرد بقى له فلاتره، ونسخة تانية من نفس
  المنطق كانت **درس R1 حرفيًا**: حارس `document.contains` يتصلح في واحدة
  ويفضل مكسور في التانية، **والفلتر يبقى multi-select بالاسم بس بلا أي
  رسالة خطأ**.
- ⚠️ **ومفاتيح نطاق `q` ما اتغيّرتش بحرف** — نفس الـ ids القديمة بالظبط
  (`qMsBtn-courier` · `qChipsRow-where` …)، و`qMsState` بقى **مرجع** لحالة
  النطاق ده.
- ⚠️ و`qSyncMsItems()` بتغذّي **النطاقين** من نفس `qState.rows` — والسطر
  بتاع `aud` بيتخطّى لوحده في صفحة المشحون (`MS.aud` مش موجود هناك).

### 🔴 فلتر «التغليف» — مصدره `packedAt` نفسه

```js
{ key: 'packed', label: 'التغليف', of: o => o.packedAt ? 'تم التغليف' : 'لم يتم التغليف' }
```

- 🔴 **نفس الحقل اللي العمود بيعرضه وبيترتّب بيه بالحرف** — فمستحيل صف
  يتقري «اتغلّف» في العمود و«ما اتغلّفش» في الفلتر.
- ⛔ **وممنوع يتحسب من `packedByS1`/`packedByS2`** — أوردر اتغلّف والاسم ما
  اتسجّلش كان هيبقى «ما اتغلّفش» وهو اتغلّف.
- ⚠️ **وفي الطابورين** — العمود موجود في الاتنين، والحقل بيرجع من الـ
  Workers الاتنين.

### 🔴 مقاسات الأعمدة — `width` + `max-width` مع بعض

```css
th.col-cust, td.cust-cell { width: 90px;  max-width: 90px; }
th.col-date, td.date-cell { width: 112px; min-width: 112px; max-width: 112px; }
th.col-addr, td.addr-cell { width: 320px; max-width: 320px; }   /* الجاهز بس */
th.col-note, td.note-cell { width: 250px; max-width: 250px; }   /* الجاهز بس */

/* v1.8.0 — الحشو الأضيق والسطر الواحد (الشرح في قسم «تمريرة v1.8.0») */
td.date-cell { padding-inline: 6px; }
.date-cell .cell-date { display: block; white-space: nowrap; font-size: 13.5px; }
```

**المقيس على عرض الكارت الكامل (1400):**

| العمود | قبل | بعد |
|---|---|---|
| العميل | ١٣٨ | **٩٠** (−٣٥٪) |
| تاريخ الأوردر | ١٢٧ | **١١٢** |
| تاريخ التغليف | ١٢٨ | **١١٢** — متساويان بالحرف |
| العنوان | ٢٨٠ | **٣٢٠** |
| ملحوظات | ٢١٢ | **٢٥٠** |

- 🔴 **`max-width` لوحدها مش كافية** — في `table-layout:auto` هي **تلميح
  بيتجاهَل** لما يبقى فيه فراغ زيادة يتوزّع (العنوان كان **٢٨٠** وسقفه
  مكتوب `260`). و`width` لوحدها بتتزوّد بنفس التوزيع. الاتنين مع بعض هما
  اللي بيثبّتوا العمود فعلاً.
- 🔴 **و`min-width` على عمودَي التاريخ مش تزويق** — على شاشة أضيق من
  الكارت المتصفح بيضغط الأعمدة المرنة وبيسيب اللي محتواه مش قابل للضغط:
  «تاريخ الأوردر» كان بينزل تحت الـ112 و«تاريخ التغليف» بيقف عندها (بادج
  التأخير أطول من بادج العمر)، فالعمودان يفترقوا تاني **من ورا عرض
  الشاشة** — وده بالظبط اللي التمريرة دي اتعملت ضده.
- ⚠️ **ومقاسات «العميل» و«التاريخ» على الصفحتين** — نفس الجدول ونفس
  المعيار، وجدول بيتقري بمقاسين في صفحتين بيتعلّمه الموظف مرتين. اللي في
  صفحة «الجاهز للشحن» لوحدها هو «العنوان» و«ملحوظات» (مش موجودين في
  المشحون أصلاً).
- ⚠️ **وخلية العميل بقت بتلفّ على سطرين** في الأسماء الطويلة — ده **ثمن
  معروف** للتصغير اللي اتطلب، والاسم كامل مش مقصوص.
- ✅ **وفيه تلات بنود في `docs/queues-check.mjs` بيقروا العرض المحسوب من
  العنصر نفسه** — مش اسم الكلاس: كلاس اتغيّر من غير CSS وراه بيعدّي على أي
  grep.

## 🔴 تمريرة v1.6.0 — الرئيسية والتلات تعديلات في الطابور (طلب أحمد 17-09-2026)

### ① الشاشة الرئيسية — تسمية وبس

| # | التغيير | ليه |
|---|---|---|
| ① | **سطر الشرح فوق الطوابير اتشال** | نص بيتقرا **مرة واحدة** وبياخد مساحة فوق أهم رقمين في الشاشة كل يوم |
| ② | «الطوابير» → **«قسم الحالات الانتقالية»** · «أدوات» → **«قسم أدوات العمليات»** | الاسم بيوصف **إيه ده**: الطابور حالة الأوردر وهو ماشي، والأداة فعل |
| ③④ | «جاهز للشحن» → **«أوردرات جاهزة للشحن»** · «مشحون بلا نتيجة» → **«أوردرات تحت التوصيل»**، وتحت كل واحد **كود حالته** (`Ready` · `Shipped`) | الاسم بيقول الأوردرات، والكود بيقول **القيمة الحرفية** اللي الموظف بيدوّر عليها على شوبيفاي |
| ⑤⑥ | «محدّث حالة الأوردر» → **«تحديث حالة الأوردرات»** · «مركز التحصيل» → **«تحصيل الأوردرات COD»**، وبلا وصف تحتهم | نفس السبب في ① — والوصف كان بيقول اللي الصفحة نفسها بتقوله في أول سطر |

- 🔴 **والمعلومة اللي في السطر المتشال ما ضاعتش** — التحديث التلقائي كل ١٥
  دقيقة وقاعدة «الأدوات بلا عدّاد» **مكتوبين في «عن الأداة»**، والعدّاد
  الملوّن جوّه كل صف بيقول عمر الرقم لوحده.
- 🔴 **وكود الحالة مش `.dco-card-desc`** — كلاس مستقل (`.dco-card-code` في
  `shared/shell.css`): `--font-mono` · `direction:ltr` · بنط أكبر · مربع
  بإطار. السبب إنه **قيمة** مش وصف — بنط الوصف كان بيخلّي `Ready` تتقري
  كلمة إنجليزي وسط جملة عربي بدل ما تتقري القيمة اللي على شوبيفاي.
- 🔴 **واسم الأداتين اتغيّر في هيدر الصفحتين كمان** (`docs/port-standalone.py`
  → `title` و`<title>`)، ومعاه `label` في `DCO_WORKERS`. ⛔ الاسم في الكارت
  غير الاسم في الصفحة معناه **أداة واحدة باسمين** والموظف بيتعلّمها مرتين —
  وفيه بند في `docs/tools-check.mjs` بيقارن الاتنين.
  ⚠️ **والصفحتين متولّدتين**، فالتغيير اتعمل في المولّد **وبعدين اتشغّل** —
  والبند اللي بيقارن البايتات هو اللي بيثبت كده.

### ② «موقع الشحنة» بقى بادج — أخضر للمكتب وأحمر لأي مكان تاني

```js
Office → ✅ في المكتب (أخضر) · أي قيمة تانية → ⚠ <القيمة> (أحمر) · فاضي → —
```

- 🔴 **الطابور ده أوردرات المفروض طرودها في المكتب**، فالعمود سؤال بإجابتين
  مش قايمة قيم. لون بيتقري من بعيد بيخلّي الصف المخالف يبان **قبل** ما
  الموظف يقرا القيمة.
- 🔴 **والقيمة الغلط بتتعرض بالحرف جوّه البادج** — «مكان تاني» لوحدها
  تكلفتها فتح الأوردر على شوبيفاي (قاعدة ١٤).
- 🔴 **والفاضي `—` محايد — ⛔ مش أحمر.** «محدش سجّل» **مش** «الطرد في مكان
  غلط»: أداة التغليف لسه ما بتكتبش الحقل، فأحمر هنا كان هيبقى على **أغلب
  الطابور** — والموظف بيتعلّم يعدّي على اللون كله (نفس درس `already`
  الأحمر في سكانرات بوسطة).

### ③ عمود «ملحوظات» — بعد العنوان، وبنفسجي

- 🔴 **مصدره `note` في رد `get_ready_queue`** — حقل **Notes** بتاع الأوردر
  على شوبيفاي، وجه في `ready-orders-worker` **v1.3.0** (سكالر على الأوردر
  نفسه = **صفر زيادة** في تكلفة الاستعلام).
- 🔴 **و`ready.min` اترفع لـ`1.3.0` في نفس التسليم** — على Worker أقدم
  العمود بيقول `—` على كل صف، يعني «مفيش ولا ملحوظة في الطابور» وده
  **ادعاء غلط** مش خانة فاضية.
- 🔴 **واللون بنفسجي عن قصد.** الملحوظة **استثناء** — الموظف محتاج يشوف
  **إن فيه واحدة** من قبل ما يقرا نصها. ⛔ **ومش أحمر ولا كهرماني** — دول
  لونين العلامات والتأخير في نفس الجدول، والملحوظة **مش شذوذ**.
- 🔴 **والمسافات بس == مفيش ملحوظة** (`.trim()`) — خلية فيها مسافة بتبان
  «فيها حاجة» وهي فاضية، فالموظف بيفتح أوردر مالوش ملحوظة.
- ⚠️ **والخلية بتلفّ** (`.note-cell`) زي خلية العنوان بالظبط — نص طويل على
  سطر واحد بيزقّ باقي الأعمدة برّه الشاشة.
- ⚠️ **والبحث بقى بيدوّر فيها** — عمود معروض ومش قابل للبحث بيخلّي الموظف
  يقرا الطابور صف صف.
- ⛔ **وممنوع يتضاف في «طابور المشحون»** — `shipped-orders-worker` مابيرجّعش
  `note` (نفس قاعدة عمود العنوان بالحرف)، وفيه بند في `docs/queues-check.mjs`
  بيمنع إضافته «بالقياس».

### ④ بادج الأيام **تحت** التاريخ — في العمودين

- 🔴 **كان بينزل تحته بالصدفة.** الخليتان كانتا `تاريخ <بادج>` على سطر
  واحد، والنزول تحت كان **لفّ سطر** بيعتمد على عرض الشاشة: على شاشة عريضة
  العمودان بيتقروا بشكلين مختلفين في نفس الجدول.
- ✅ **دلوقتي `.date-cell .cell-date { display:block }`** — التكديس
  **مقصود ومتطابق** في «تاريخ الأوردر» و«تاريخ التغليف».
- ⚠️ **والبند في فحص المتصفح بيقرا `display` المحسوب من العنصر نفسه** مش
  اسم الكلاس — كلاس اتغيّر من غير CSS وراه بيعدّي على أي grep.

## 🔴 تاب «جرد المكتب» (v1.3.0 · وتمريرة شكله v1.4.0 · طلب أحمد 16-09-2026)

**الفكرة:** الموظف بيعمل سكان لباركود **كل طرد في المكتب**، والشاشة بتقول
تلات حاجات: اللي **مظبوط**، اللي **مفقود** (المفروض موجود وما اتعملّهوش
سكان)، واللي **موجود خطأ** (اتعمله سكان ومش المفروض يكون هنا).

> ℹ️ **الفكرة كانت موجودة في `Ready-to-Ship` القديمة** (تاب «المراجعة»)،
> والتنفيذ هنا اتبنى على دروسها: نمط السكانر · التفسير المزدوج للكود
> (اسم/ID) · وتفريق «مش في القايمة» عن «مش موجود على شوبيفاي». اللي اتغيّر:
> الاشتقاق كله بقى في الـ shell ومتقاس، والنطاق بقى **لقطة مُعلَنة**.

| الجزء | فين |
|---|---|
| القواعد (التطبيع · المطابقة · التلات أقسام · السبب) | **`shared/shell.js` §AUDIT-RULES** |
| الشاشة والتابات والسكانر والتصدير | `docs/build-pages.py` → `AUDIT_*` (صفحة الجاهز بس) |
| الحالة الحقيقية للطرد الغلط | `ready-orders-worker` **v1.2.0** → `lookup_orders` |
| الفحص | `docs/rules-check.mjs` §⑧ · `docs/queues-check.mjs` §⑨ |

### 🔴 النطاق = **فلاتر التاب دي لوحدها** — ولقطة وقت البدء

🔴 **اتغيّر في v1.7.0 (طلب أحمد 17-09-2026).** قبل كده النطاق كان **الصفوف
المعروضة بعد فلتر تاب «الأوردرات»**؛ دلوقتي التاب دي ليها **فلترينها هي**
فوق مربع السكان — **خطوة ①** قبل «بدء الجرد»:

| الفلتر | القيم | ليه |
|---|---|---|
| **المندوب** | مناديب · بوسطة · شو روم | 🔴 **مجموعة مش اسم** — الجرد بيتعمل على **رفّ**، والرف مقسوم بالمجموعة. قايمة بأسماء المناديب كانت هتخلّي الخطوة الأولى أطول من الجرد نفسه |
| **موقع الشحنة** | نفس قيم العمود (✅ في المكتب · ⚠ المخزن · ⚠ مع المندوب · `—` محدش سجّل) | «اجرد اللي المفروض في المكتب بس» سؤال الجرد نفسه |

- 🔴 **ليه الفصل:** الفلترين وظيفتهم مختلفة — الموظف بيفلتر الطابور عشان
  **يقرا**، وبيفلتر الجرد عشان **يمسك السكانر**. وربطهم كان معناه إن ضغطة
  فلتر للقراءة تحدد نطاق جرد جاي، فقايمة «مفقودة» تطلع غلط **بلا أي تفسير
  على الشاشة** — والموظف اللي بيسكن مش شايف فلتر التاب التانية أصلاً.
- ⚠️ **والمصدر لسه واحد** (`qState.rows`) — نفس الطابور اللي التاب التانية
  بتعرضه، فمستحيل الجرد يشوف أوردر مش في «قسم الجاهز للشحن».
- ⚠️ **واختيار فاضي = الكل** — «ما اخترتش» ≠ «مفيش»، زي أي فلتر في الهب.
- 🔴 **والفلاتر بتتقفل مع «بدء الجرد»** (`msSetLocked('aud', true)`)
  وبتتفك مع «جرد جديد» — **من غير ما تتمسح**: الموظف اللي بيجرد «بوسطة»
  رفّ ورا رفّ عايز اختياره مكانه. وسطر **«🔒 النطاق اتثبّت وقت البدء»**
  بيظهر تحتهم.
  ⛔ **وحارسان مش واحد** — `disabled` على الزرار، وفحص `locked` جوّه
  `msToggleItem`/`msClear`/`msToggleSelectAll` نفسهم. الأول لوحده بيتخطّى
  بأي نداء مباشر.
- 🔴 **والحساب الحيّ من الفلتر كان بيكسر الأداة:** ضغطة فلتر وسط الجرد كانت
  بتغيّر **قايمة المفقود فجأة** — رقم غلط شكله سليم، وبلا أي خطأ.
- 🔴 **واللقطة `map(r => ({...r}))` مش مرجع** — `qState.rows` بتتبدل
  بالكامل مع كل جلب (كل ١٥ دقيقة)، والمرجع كان هيخلّي النطاق يتغيّر تحت
  إيد الموظف.
- 🔴 **والنطاق مكتوب على الشاشة — بس في العنوان دلوقتي** (v1.4.0 · طلب
  أحمد): «جرد أوردرات (مناديب ٧٢ + شو روم ١٥)»، مشتق من **اللقطة نفسها**
  بـ`dcoCourierCounts` — نفس الدالة اللي مربعات الطابور بتعدّ منها، فالرقمين
  مايقدروش يفترقوا. السطر الشارح القديم («النطاق هيبقى المعروض دلوقتي بعد
  الفلتر…») **اتشال بطلب أحمد**.
  ⚠️ ومن v1.7.0 بيتحسب من **`audVisible()`** قبل البدء (فلاتر التاب دي)
  بدل `qVisible()`.
  ⚠️ **والفلتر الكامل بالنص لسه مكتوب في `audState.scopeLabel`** وبيتصدّر في
  **شيت «الملخص»**. ملف بلا نطاقه بيتقري «الجرد كله» بعد أسبوع من تصديره —
  والبند ده مقفول في `docs/queues-check.mjs`.
- 🔴 **وتحذير «برجاء عدم تحديث حالة أي شحنة أثناء الجرد» تحت العنوان** —
  وبيفضل معروض **طول ما الجرد شغّال**، وبيختفي بعد الإنهاء بس. السبب مش
  تنبيه عام: النطاق لقطة، فالشحنة اللي حالتها اتغيّرت وسط الجرد بتفضل في
  النطاق و**بتتعدّ «مفقودة»** وهي مش مفقودة.
- ⚠️ **والتحديث التلقائي مابيتوقفش** أثناء الجرد — ومالوش أثر: النطاق لقطة،
  وتصنيف كل سكانة بيتحدّد **وقت السكانة** وبيتخزّن معاها (مش بيتحسب تاني).

### 🔴 المربعات الأربعة — أسماؤها وترتيبها وفلترتها

الترتيب والأسماء **طلب أحمد (v1.4.0)**، والترتيب على الشاشة من اليمين:

| # | المربع | بيقول إيه |
|---|---|---|
| ① | **إجمالي الأوردرات** | حجم النطاق (كان «نطاق الجرد») |
| ② | **أوردرات متبقية** → **مفقودة** | كان «لسه ما اتعملّهاش سكان» — والاسم بيتغيّر بعد الإنهاء |
| ③ | **حالة سليمة ✅** | كان «مضبوطة» |
| ④ | **حالة خطأ ⛔** | كان «موجودة خطأ» |

- 🔴 **«أوردرات متبقية» ≠ «مفقودة» — لسه مربعين، وواحد بس بيتعرض في المرة.**
  الأول **حالة وسط الجرد** والتاني **نتيجة بعد ما يتقفل**. مربع «مفقودة»
  والموظف لسه بيسكن بيخلّيه يدوّر على طرد في إيده.
- 🔴 **وبعد «إنهاء الجرد» المربعات بقت فلاتر** (v1.4.0): ضغطة على مربع بتفتح
  قسمه **وبتطوي التانيين**، و«إجمالي الأوردرات» بيفتح التلاتة، والضغطة
  التانية على نفس المربع بتطوي كله.
  ⛔ **ووسط الجرد الضغطة مابتعملش حاجة** — الأقسام نتيجة مش حالة.
  ⚠️ **والمربع المختار بياخد إطار** (`.aud-card.active`) — قايمة اتقفلت من
  غير ما المربع يقول إنه هو السبب بتتقري **«القسم فاضي»**.
  ⚠️ **والطي اليدوي من هيدر القسم بيصفّر المربع المختار** — قسم مفتوح بالإيد
  ومربع تاني مولّع حالتان بتفترقا على نفس الشاشة.
- ⚠️ وقبل البدء العدّادات **`—` مش `0`** — «ما اتجردش» ≠ «مفيش».

### 🔴 التلات أقسام — **جدول واحد بنفس أعمدة الطابور** (v1.4.0)

طلب أحمد: التلات قوايم تبقى **جدول زي جدول تاب «الأوردرات» بالظبط** — نفس
العشرة أعمدة بنفس الترتيب (رقم الأوردر · العميل · العنوان · **ملحوظات** ·
المندوب · موقع
الشحنة · تاريخ الأوردر · تاريخ التغليف · نوع الأوردر · مراجعة)، ونفس دوال
الخلايا (`addressCell` · `whereaboutsCell` · `dcoWaiting`).

- 🔴 **وقسم «موجودة خطأ» بقى جدول كمان** — كان كروت. والسبب اللي الكروت
  اتختارت عشانه (نص السبب بيلف والجدول بيخلّي الخلية تاكل عرض الشاشة)
  اتحلّ بـ**صف تاني تحت الصف مباشرةً بعرض الجدول كله** (`colspan`): السبب
  والتفصيل والفعل مكتوبين هناك، **مش وراء ضغطة**.
  ⛔ والفرق مش تجميلي — قاعدة ١٤: بند بيقول «فيه حاجة» وبس تكلفته **فحص
  يدوي لكل طرد** على شوبيفاي.
- 🔴 **والكود اللي مالوش أوردر بياخد نفس الصف بخانات `—`** — صف بشكل تاني
  وسط الجدول بيتقري «ده مش من نفس القايمة».
- ⚠️ **وبلا `sortable-th` عن قصد** — رؤوس الطابور بتنادي `qToggleSort` وهي
  بترتّب **الطابور**، فحطّها هنا كان هيرتّب جدول تاني تحت إيد الموظف.
  الشكل واحد والسلوك مفصول.
- ⚠️ **وعناوين الأقسام بقت أقصر وبلا عدّاد جنبها** (طلب أحمد): «✅ أوردرات
  مظبوطة» · «❌ أوردرات مفقودة» · «⚠️ أوردرات موجودة خطأ». الرقم في المربع
  فوق، وتكراره في مكانين بيخلّي أي فرق بينهم **بلا تفسير**.
- ⚠️ **وشاشة البدء بلا نص شارح** — العنوان بيقول اللي هيتجرد، والزرار بيقول
  الخطوة، والشرح الكامل في «عن الأداة». نص شارح بيتقرا **مرة واحدة** وبياخد
  مساحة كل يوم.

### 🔴 «موجودة خطأ» — كل مدخل بيقول السبب والفعل

قاعدة ١٤ حرفيًا: بند بيقول «فيه حاجة» وبس تكلفته **فحص يدوي لكل طرد** على
شوبيفاي. فكل كود في القسم ده بياخد `label` + `detail` + `action`:

| الحالة | معناها | الفعل |
|---|---|---|
| `out_of_scope` | الأوردر **`Ready` فعلاً** بس بره الفلتر اللي اتثبت | الطرد **مكانه صح** — وسّع النطاق لو عايز تعدّه |
| `unknown` | مش في القسم، **والاستعلام التلقائي فشل** | اضغط «إعادة محاولة الاستعلام» |
| `not_found` | استعلمنا ومفيش أوردر بالكود ده | ملصق قديم أو طرد أداة تانية |
| `cancelled` | الأوردر **ملغي** | رجّع المنتجات على الرف |
| `already_shipped` | `S1`/`S2` = `Shipped` والطرد في المكتب | راجع خطوة الشحن |
| `closed` | `Delivered`/`Returned` | مرتجع أو ملصق قديم |
| `not_ready` | أي حالة تانية — **بتتعرض بالحرف** | حدّد الحالة الصح أو رجّع الطرد |

- 🔴 **`out_of_scope` سبب مستقل عن قصد** — دمجه في «الطرد ده غلط» كان بيدّي
  الموظف **حكم غلط على طرد سليم**، وحكم غلط أسوأ من مفيش حكم.
- 🔴 **والإلغاء بيغلب أي حالة تانية** — نفس ترتيب `dcoFlags` بالظبط.
- 🔴 **والاستعلام بقى تلقائي مع «إنهاء الجرد»** (v1.4.1 · طلب أحمد) —
  «الفعل المطلوب» في القسم ده كان بيقول للموظف «اضغط استعلام عن الحالة
  الفعلية»، يعني **خطوة يدوية بين النتيجة وقراءتها**. دلوقتي الشاشة بتوصل
  للحالة الحقيقية لوحدها بمجرد ما الجرد يتقفل.
  ⚠️ **والقاعدة القديمة ما اتكسرتش** — هي اتكتبت ضد التلقائي **وسط شغل
  السكان** (نداء شبكة على كل كود غلط وإيد الموظف على السكانر)، واللي بيحصل
  هنا **نداء واحد مجمّع بعد ما السكان يقف**. ⛔ وممنوع يرجع جوّه `audScan`.
  🔴 **والفشل بيبان** — الزرار بيفضل مكانه بنص **«إعادة محاولة الاستعلام»**
  والرسالة بتسمّي النسخة المطلوبة (`1.2.0`). «استعلام عن الحالة الفعلية»
  على زرار ظهر بعد محاولة فاشلة بيتقري «ما اتحاولش»، فالموظف يستنى حاجة
  مش جاية. وفيه كتلة كاملة في فحص المتصفح بتقفل ده.
  ⚠️ **و`audEnd` مابتنتظرش النداء** (بلا `await`) — الانتظار كان بيأخّر رسم
  النتيجة كلها لحد ما شوبيفاي ترد.

### الباركود — Order ID الطويل (قرار أحمد 16-09-2026)

```js
dcoScanParse('6959895839042') → { kind:'id',   id:'6959895839042', name:'#6959895839042' }
dcoScanParse('#55001')        → { kind:'name', id:null,            name:'#55001' }
```

- 🔴 **التفسيرين بيرجعوا مع بعض** والمطابقة بتجرّب الاتنين — العتبة (١٠
  أرقام) لوحدها كانت بتضيّع **أوردر اسمه رقم طويل** (وده موجود فعلاً في
  المتجر، ونفس الحالة اللي الأداة القديمة عالجتها).
- 🔴 **والمقارنة بالاسم بعد شيل `#`** — نفس قاعدة مربع البحث. المطابقة
  الحرفية كانت بترجّع «مش موجود» على أوردر قدام الموظف في الجدول.
- 🔴 **ورقم التتبع بيطابق بالتساوي الكامل** مش `includes` — `123` على
  `1234567` هو الفخ المقيس في فحص الباركود بتاع هب المخزن.
- ⛔ **وكود أقل من ٤ أرقام مابيتحسبش سكانة** — دي قراءة مقطوعة من السكانر،
  وجزء من الكود ممكن يطابق أوردر تاني.
- 🔴 **والتفريد على `orderId` مش على الكود** — نفس الأوردر بالاسم مرة وبالـ
  ID مرة كان بيتعدّ **مرتين**، فـ«مضبوطة» تبقى أكبر من النطاق نفسه.
- ⚠️ **ونغمة المكرر محايدة** (`playBeep('scan')`) مش إنذار — التكرار حالة
  عادية في جرد مئات الطرود، ونغمة إنذار على كل تكرار بتعلّم الموظف يعدّي
  على الإنذار كله (درس `already` الأحمر).

### الحفظ والتصدير

```js
const DCO_AUDIT_READY = 'dco_audit_ready';   // sessionStorage
```

- 🔴 **الجرد محفوظ في الجلسة** — `refresh` وسط جرد ١٢٦ طرد كان بيمسح ساعة
  شغل **بلا أي تحذير**. الحفظ بعد **كل سكانة** وبعد كل تغيير حالة.
- ⚠️ **وقفل التاب بيمسحه** — ده ثمن مُعلَن (مكتوب في «عن الأداة»): السر هو
  **الحاجة الوحيدة** في `localStorage` في الريبو كله (#28 · #39).
- ⚠️ والشكل بيتفحص بند بند عند الرجوع — جلسة بشكل قديم **بتترفض** بدل ما
  تتقري غلط (نفس قاعدة `v` في عقد الجلسة).
- 🔴 **ExcelJS من CDN — أول مكتبة خارجية في الهب كله** (قرار أحمد). وكل
  استخدام **محروس بـ`typeof ExcelJS === 'undefined'`**: CDN محجوب كان
  بيخلّي الضغطة ترمي `ReferenceError` **وتسكّت باقي السكربت** — الصفحة
  بتفضل مفتوحة وكل زرار بعدها مابيعملش حاجة. وفيه بند في فحص المتصفح
  بيقفل ده.
  🔴 **والجملة دي كانت ادعاء غلط لحد 17-09-2026 — ولازم يتقال.** الحارس كان
  في `ready-orders.html` **بس**، والصفحتين المدموجتين كانوا بينادوا
  `new ExcelJS.Workbook()` **عاري**. والمقيس وقتها: «تصدير المحدد» في
  الصفحتين **صامت تمامًا**، و«تصدير الكل» في التحصيل آخر رسالة شافها الموظف
  «⏳ جاري تجهيز التصدير...» وبعدها سكوت — **ادعاء شغل شغّال على تصدير مات**.
  ⚠️ **والعطل موروث من الريبوهات الأصلية** مش من الدمج، فالإصلاح اتعمل
  **هناك** (`Order-Status-Updater` · `COD-Payment-Center`) والمولّد اتشغّل.
  ✅ **ودلوقتي الجملة صحيحة ومقفولة بأربع بنود** في §⑦ بتحجب الـ CDN فعلاً.
- ⚠️ **والتصدير أربع شيتات**: مظبوطة · مفقودة · موجودة خطأ (بالسبب والفعل)
  · **الملخص** (النطاق والفلتر والوقت والموظف). ملف بلا نطاقه بيتقري
  «الجرد كله» بعد أسبوع من تصديره.

### ⛔ ومفيش جرد في «طابور المشحون»

أوردر `Shipped` **خرج من المكتب بالتعريف**، فجرده سؤال مالوش معنى. لو اتقرر
يوم، لازم الأول يتقرر **إيه المفروض يكون موجود** هناك — وده قرار أحمد مش
قرار كود. وفيه بند في فحص المتصفح بيمنع إن التاب يتحقن هناك «بالقياس».

## `?action=` اللي الهب بيستخدمها

| الـ Worker | الـ actions |
|---|---|
| **الدخول (الهب)** | `get_config` · `diag` · `get_employees` · `check_employee` · `register_pin` · `verify_employee` (POST) · `log_logout` — **كلهم بلا `appId`** |
| **الجاهز للشحن** | `get_config` · `diag` · **`get_ready_queue`** · **`lookup_orders`** (POST · من v1.2.0) |
| **المشحون** | `get_config` · `diag` · **`get_shipped_queue`** |
| **محدّث حالة الأوردر** 🔗 | `get_config` · `diag` · `resolve_orders` · `resolve_by_name` · `search_courier_orders` · `order_statuses` · `courier_values` · `reason_values` · `order_details` · **`update_status`** (كتابة) · `get_logs*` |
| **مركز التحصيل** 🔗 | `get_config` · `diag` · `get_logs*` · `audit_pending` · و**POST + `action` في الجسم**: `preview` · `resolveByName` · **`pay`**/**`refund`** (كتابة) · `getCourierValues` · `getCourierOrders` · `getCourierOrdersToday` |

> 🔴 **و`lookup_orders` مالهاش مستهلك غير تاب الجرد** — قراءة بحتة بتجيب
> حالة أوردر بالـ ID (بـ`nodes(ids:)` مش ببحث) أو بالاسم (ببحث + مطابقة
> حرفية). ⛔ ومن غير المستهلك ده كان ممنوع تتضاف من الأصل.

> 🔴 **طابورا الشحن مالهمش `get_logs*` ولا `get_employees` ولا أي نقطة
> دخول** — مابيكتبوش في D1 أصلاً فمفيش سجل يتفلتر، والدخول بيحصل **مرة
> واحدة** في `index.html`. نقطة endpoint بلا مستهلك = سطح بلا لازمة (نفس قرار
> `order-sku-barcode-printer-worker` و`package-transfer-to-office-worker`).
> ⚠️ **والأداتين المدموجتين عكسهم تمامًا** — بيكتبوا في D1 وليهم `get_logs*`
> وتاب سجل كامل. الـ endpoints دي **كانت موجودة قبل الدمج** ومالهاش أي
> علاقة بيه؛ اللي اتشال منهم هو نقط **الدخول** بس (`check_employee` ·
> `register_pin` · `verify_employee` · `get_employees`) — مابقاش ليها
> مستهلك في الواجهة بعد ما شاشة الدخول اتشالت.
> ⛔ **ومع ذلك ما اتشالتش من الـ Workers** — الريبو القديم لسه بيستخدمها
> (قرار ③)، وشيلها كان هيكسر الرابط القديم.

## 🔗 الأدوات المدموجة (v1.5.0 · طلب أحمد 16-09-2026 — واتضافت `partial-delivery.html` في v1.10.0)

**الطلب الأصلي:** «أضيف وادمج الأداتين Order Status Updater و COD Payment
Center تحت مركز الشحن والتحصيل». والقرارات التلاتة اللي التنفيذ اتبنى عليها
(أحمد · 16-09-2026):

| # | القرار | البديل اللي اتّرفض |
|---|---|---|
| ① | **الصفحتان بينتقلوا للهب + دخول واحد** | مجرد كروت بتفتح الروابط القديمة (كان هيسيب تلات دخولات وتلات أسرار) |
| ② | **الـ Workers بتنضم لمجموعة `delivery_cod_ops`** | الهب يخزّن تلات أسرار في شاشة الإعدادات |
| ③ | **الروابط القديمة تفضل شغّالة زي ما هي** | تحويل تلقائي من الريبو القديم · أو تفضية الريبو |

> 🔴 **والـ Workers ما اتلمسوش ولا سطر.** كل واحدة لسه Worker واحد في ريبو
> واحد (قرار ٨) — الهب ضمّ **الواجهة** بس، بنفس شكل الطابورين بالظبط.
> ⛔ يعني **مفيش Promote ليهم من هنا**، وأي تعديل في `index.js` بتاعهم
> مكانه ريبوهم.

### 🔴 v1.10.0 — التسليم الجزئي: نفس قرار ①② بالحرف، وعكس ③ تمامًا

طلب أحمد (20-09-2026): «ادمج الأداة وافتحها داخل مركز الشحن، وامسح الرابط
الخارجي تمامًا — الريبو المستقل هيكون Worker بس». يعني قرارين ①② فوق
اتطبّقوا بالحرف (صفحة واحدة جوّه الهب + دخول واحد · الـ Worker انضمّ
لمجموعة `delivery_cod_ops`)، **وقرار ③ اتعكس بالكامل**:

- 🔴 **مفيش نسخة قديمة تفضل شغّالة.** `Partial-Delivery/index.html` **اتشال
  خالص** من الريبو المستقل — مش «فضل شغّال بشاشة دخوله» زي order-status/
  cod-payment. الريبو المستقل بقى **Worker + `wrangler.toml` بس**.
- 🔴 **ومعناه إن `Partial-Delivery.html` هنا مش متولّدة بسكربت.** لا بانر
  «ممنوع تتعدّل بالإيد» ولا `docs/port-standalone.py` — التعديل على منطق
  الأداة بيتعمل **في الملف ده مباشرة**، لأن مفيش نسخة تانية تفترق عنه
  (درس R1 مش منطبق هنا: مفيش نسختين أصلاً).
  ⚠️ **وده قرار متعمَّد مش سهو** — `docs/tools-check.mjs` بيفلتر بند
  «الصفحة متولّدة فعلاً» على الأدوات اللي ليها `source` بس (راجع الملف)،
  عشان غياب مصدر لـ`Partial-Delivery.html` ما يوقفش الفحص ده للتنين
  التانيين.
- 🔴 **والتحويل اتعمل بالإيد على نفس نمط `port-standalone.py`** (مش
  بتشغيله، لأنه مقفول على أداتين معروفتين بالاسم ومحتاج مصدر جنب الهب):
  شاشة الدخول وشاشة الإعدادات ومودالات About/Changelog القديمة اتشالوا،
  والصفحة بقت بتاخد الجلسة والسر والهيدر والتوست من `shared/shell.js`
  بالظبط زي التنين التانيين. القرارات المعمارية والقيود (اتنين → اتلغى؛
  ⛔ الحذف مالوش استرجاع؛ نافذة التأكيد الإلزامية) **ما اتلمسوش**.
- ✅ **ومتّحقَّق منه فعليًا في نفس التمريرة** — `docs/tools-check.mjs`
  اتوسّع لتلات أدوات (كان بيقيس اتنين بس) واتشغّل حيًا: **106 بند عدّى ·
  صفر فشل**، ومعاه `docs/rules-check.mjs` (٩٠ بند) و`docs/queues-check.mjs`
  (٢٧٠ بند) اتشغّلوا تاني للتأكد إن التعديلات في `shared/shell.js` و
  `index.html` ما كسرتش حاجة في الطابورين أو الأداتين التانيين — **صفر
  فشل في التلاتة**.
  ⚠️ **وده تحقّق أضيق من الـ ٧٨ بند الأصلية** — البنود الجينيريك (الجلسة ·
  السر · الشِل · كارت الرئيسية · رسايل الفشل · حارس CDN) بقت بتغطّي
  التلاتة تلقائيًا لأنها بتلف على مصفوفة `TOOLS`، لكن بنود منطق خاصة
  بأداة بعينها (زي فحص `apiPostLegacy` بتاع التحصيل) **ما اتكتبش مقابلها**
  للتسليم الجزئي — الأداة دي أبسط (بلا راوتينج مختلط ولا حسابات توقيت
  خاصة)، فمفيش بند مكافئ كان ناقص فعليًا وقت الكتابة.

### 🔴 القرار ③ هو أخطر واحد فيهم — **نسختين من نفس الواجهة على الهوا**

ده **درس R1 حرفيًا**: «نسختان بتتعدّلا بالإيد بيفترقا مع أول تعديل، واتصلح
في واحدة وفضل مكسور في التانية لشهور». وهو بالظبط اللي حصل بين
`Ready-to-Pack` و`Orders-Packing-Checker`.

**اللي بيقلّل الخطر:** نسخة الهب **متولّدة** مش مكتوبة:

```bash
# لازم الريبوهات التلاتة تكون جنب بعض
python3 docs/port-standalone.py     # بيولّد Order-Status-Updater.html · COD-Payment-Center.html
node docs/tools-check.mjs
```

- ⛔ **ممنوع تعديل `Order-Status-Updater.html` أو `COD-Payment-Center.html`
  بالإيد** —
  التعديل بيضيع في صمت مع أول تشغيل، وفيه بانر في أول كل ملف بيقول كده.
  **التعديل بيتعمل في الريبو الأصلي، وبعدين المولّد بيتشغّل.**
- 🔴 **وكل تحويل في المولّد مربوط بـ«مرساة» نصّية، والمرساة الغايبة
  بتوقّف السكربت بصوت عالي.** ده مقصود: لو تعديل في الريبو الأصلي حرّك
  كتلة، السكربت **بيقف** بدل ما يولّد صفحة ناقصها الجزء ده — صفحة
  بشاشة دخول متشالة **نص شيلة** أسوأ من سكربت واقف.
- ✅ **وفيه بند في `docs/tools-check.mjs` بيشغّل المولّد ويقارن البايتات** —
  فتعديل يدوي على الملف المتولّد بيتمسك في الفحص مش بعد ما يضيع.
- 🔴 **و`forbid` بيقرا الماركب بس — ودي تغرة كلّفت عطل كامل (17-09-2026).**
  `p.forbid(r'id="loginOverlay"')` بيتأكد إن شاشة الدخول **اتشالت من
  الماركب**، وبيعدّي على `document.getElementById('loginOverlay')` اللي
  فاضل في السكربت. النتيجة `null` ثم `TypeError` بيقطع الدالة **في وش
  الموظف بلا أي رسالة** — وده اللي قفل عمود «سبب الإلغاء/الإرجاع» بالكامل
  (الشرح في قسم فحص عقد الدمج).
  ✅ **والحارس الجديد `check_dom_ids`** بيقرا **الناتج نفسه**: كل
  `getElementById('X')` لازم يلاقي `X` في ماركب الصفحة المتولّدة أو في
  الـ ids اللي الـ shell بيحقنها.
  ⚠️ **وقايمة ids الـ shell بتتقرا من `shared/shell.js` مش مكتوبة في
  المولّد** — قايمة يدوية بتقدم من غير ما حد يلاحظ، وساعتها الحارس يطلّع
  **تحذير كاذب** على عنصر موجود فعلاً.
  ⚠️ **وبيقرا الليترالات بس** — `getElementById(v)` أو بـ`${…}` قيمتهم
  وقت التشغيل، ونمط متساهل كان هيبقى ضوضاء بتتتجاهل.
  ⛔ **وهو مش بديل عن بنود الشاشة** — الحارس بيمسك المرجع المعلّق، وبنود
  §⑤ بتمسك **الأثر** (دالة اتقطعت، ونداء ما اتبعتش).
- ⚠️ **والنسختين بينادوا نفس الـ Worker** — يعني أي فعل من أي نسخة بيتسجّل
  في **نفس السجل**، ومفيش خطر «داتا متفرّقة». الخطر الوحيد إن **الشكل**
  والإصلاحات يفترقوا.

### اللي اتغيّر في كل صفحة — وإيه اللي ما اتغيّرش

| اتشال | بقى من | ليه |
|---|---|---|
| شاشة الدخول كلها (`§AUTH-JS` · `.login-*` · `.pin-*`) | `requireSession()` في الـ shell | دخول واحد للمركز |
| `WORKER_URL` · `LS_SECRET` · `ADMIN_WORKER_URL` | `DCO_WORKERS.*` + سر المجموعة | سر واحد · ورابط ثابت في الـ shell |
| `TOOL_VERSION` · `MIN_WORKER_VERSION` · `cmpVersion` · `checkWorkerVersion` · `showWorkerStale` · `renderVersionUI` | الـ shell | نسخة واحدة للهب (#24) |
| مودال الإعدادات · `runDiag`/`diagRows` · `showToast` · `openAbout`/`openChangelog` | الـ shell | شِل واحد |
| كتلة التوكنز (٤٧ و٥٦ متغيّر) + الـ chrome CSS | `shared/shell.css` | قاعدة «التوكنز في الـ shell بس» |
| `apiRequest`/`apiGet`/`apiPost`/`apiPostLegacy` | `dcoApi(DCO_WORKERS.*)` | **مهلة صريحة** ورسايل بتسمّي الأداة |
| `playBeep` + دوال التاريخ المكررة | الـ shell | نسخة واحدة + حارس `Invalid Date` |

> ✅ **ومنطق الأداتين ما اتلمسش ولا سطر** — نفس الخطوات ونفس المانفيست ونفس
> `pay`/`refund` ونفس السجل. اللي اتغيّر **الإطار** بس.

> 🔴 **واللي فضل في الصفحة مقصود مش سهو:**
> · **نافذة التأكيد** (`showConfirm` · `.confirm-*`) — الـ shell **مالوش
>   واحدة خالص**، والأداتين بيستخدموها قبل كل فعل لا رجعة فيه. شيلها كانت
>   هتخلّي الزرار يشتغل **بلا أي تأكيد**.
> · **`shopifyOrderUrl`/`orderLink` بتوع الأداة** — بتشيل `gid://` من الـ ID
>   وبتحط `#` قدام رقم الأوردر. نسخة الـ shell مابتعملش الاتنين، واستخدامها
>   كان بيدّي **رابط باظ** على أي ID جاي كـ gid.
> · **بانر «الأداة غير مضبوطة»** في `COD-Payment-Center.html` — بيتعرض **جوّه
>   الشاشة** قبل أي نداء، فالموظف يعرف قبل ما يمسك السكانر.
> · **`apiPostLegacy`** (اسمها في الـ shell `apiPostRoot`) — الراوتينج
>   المختلط بتاع `cod-payment-center-worker`: `action` في **جسم** الطلب
>   لـ`pay`/`refund`/`preview`/`getCourierOrders`. ⛔ وممنوع «نتوحّد»
>   بتحويلها لـ`apiPost` — الـ Worker بيرجّع «action غير معروف» على **فعل
>   مالي**. وفيه أربع بنود في `docs/tools-check.mjs` بيقفلوا ده.

### 🔴 استثناء `--container-max` — أول كتلة توكنز في صفحة

الصفحتان فيهم **تاب سجل**، يعني **Tier M (1200)** مش Tier L (1400) بتاع
طوابير الهب. فكل صفحة فيها سطر واحد:

```css
:root { --container-max: 1200px; }
```

- ⚠️ **ودي المرة الأولى في الريبو ده** — الجملة اللي كانت في القاعدة
  («مفيش استثناء دلوقتي — التلات صفحات كلها Tier L») كانت بتوصف الوضع وقتها،
  والقاعدة نفسها بتسمح بالاختلاف لما الـ Tier يختلف فعلاً.
- 🔴 **وسطر واحد ≠ كتلة توكنز.** وفيه بند في `docs/tools-check.mjs` بيقرا
  كل `:root` في الصفحة وبيفشل لو فيها **أي متغيّر تاني** غير ده.

### 🔴 عطلان اتصلحوا في التمريرة — مكسب مجاني من الدمج

- 🔴 **`cod-payment.html` كانت شايلة إزاحة توقيت ثابتة** —
  `const CAIRO_OFFSET_HOURS = 3;` ومعاها كومنت بيقول حرفيًا «Egypt DST ends
  29-10-2026 → change to 2». يعني الإصلاح كان **خطوة يدوية متعلّمة على
  تاريخ**، ولو اتنسيت كل وقت معروض في السجل وكل حدود «فترة سريعة» وكل حساب
  «اليوم/أمس» كانوا هيغلطوا **بساعة** والأداة **مابتشتكيش**. بقت محسوبة
  بـ`Intl` على `Africa/Cairo` من الـ shell.
  ✅ **وبندان في `docs/tools-check.mjs` بيقيسوا الإزاحة فعليًا** قبل
  29-10-2026 (+٣) وبعدها (+٢) — مش بيقروا الكود.
- 🔴 **ومفيش مهلة على أي نداء في الأداتين.** `dcoApi` بيجيب مهلة **٩٠
  ثانية**. من غيرها الـ Worker الواقف بيسيب الزرار معطّل والدايرة بتلف
  **للأبد**، والموظف بيفتكر إن الأداة اتعلّقت فبيعمل refresh — وده بالظبط
  اللي بينتج «فشل كذّاب» على **تحصيل اتنفّذ فعلاً**.

### ⚠️ الأثر في D1 — لازم يتقال

صف الدخول بقى بيتكتب باسم **الهب** (`delivery_cod_ops_center` · `login`)
مش باسم `order_status` ولا `cod_payment`.

- ⚠️ يعني عدّادات `login` بتوع الأداتين **بتقف عند رقمها** من 16-09-2026.
  **مش عطل** — بس أي استعلام بيعدّ دخول الأداتين لازم يعرف.
- ✅ **وصفوف الشغل ما اتغيّرش فيها حاجة**: `update` و`payment` و`refund` لسه
  فيها `employee` باسم الموظف، والقيم زي ما هي.
- 🔴 **والعقد العابر للأدوات ما اتلمسش ولا مفتاح** —
  `cod-payment-center-worker` بيقرا صفوف `order_status` من D1 مباشرة
  (`extra.result` · `extra.courier` · `extra.targetLabel` · `extra.specifier`)،
  والدمج **ما غيّرش حرف** في `§CONTRACT::extra` ولا في `TOOL_NAME`.
- ⚠️ و`log_logout` بيروح على **Worker الهب** مش على Workers الأداتين — نفس
  السبب.

### ⛔ قبل ما الأدوات تشتغل فعلاً — خطوة يدوية واحدة (وبقت تلاتة Workers من v1.10.0)

🔴 **`WORKER_SECRET` على `order-status-updater-worker` و
`cod-payment-center-worker` و`partial-delivery-worker` لازم يتدوّر لقيمة
مجموعة `delivery_cod_ops`** من داشبورد كلاودفلير. قبل كده كل نداء من
صفحات المركز التلاتة دي بيرجّع **401**.

- ✅ **والفشل ده مش صامت:** `dcoApi` بيسمّي الأداة في الرسالة، و🩺 فحص
  النظام من الشاشة الرئيسية بيعرض الخمس Workers ويقول أنهي واحد رافض.
- ⚠️ **وحارس النسخة مابيولّعش تحذير كاذب** — `checkWorkerVersion` بتبلع
  الاستثناء عن قصد: `401` معناه «مش واصل» مش «نسخة قديمة».
- ⚠️ **والنسخة القديمة في ريبوها هتحتاج السر الجديد كمان** — نفس الـ Worker،
  فالموظف اللي بيفتح الرابط القديم لازم يلزق القيمة الجديدة في إعداداته.

## D1

```
tool  : delivery_cod_ops_center
type  : login · logout      ← بس. الهب **مابيكتبش** أي فعل تشغيلي
```

> 🔴 **الصف بيتكتب من Worker الهب نفسه** (`index.js` في الريبو ده)، والاسم
> **متحدّد في كوده** — مش بيتبعت من العميل.
> ⚠️ `AUTH_APPS` موجودة في الـ Worker بقيمة واحدة (`TOOL_NAME`) — دي الكتلة
> القياسية المنسوخة حرفيًا، والهب **مابيبعتش `appId` أصلاً**. لو اتضافت واجهة
> تانية يومًا، اسمها بيتضاف هناك **وبيترفع `auth.min` في نفس التسليم**.
> 🔴 **والبند الحاجز الوحيد الفاضل:** تسجيل `delivery_cod_ops_center` في
> `ecommoda-constants` §7 **قبل أول `writeLog`** (Rule 7).
> ✅ ودي **فرصة نادرة**: الأداة لسه ما كتبتش ولا صف، فالترتيب اللي Rule 7
> بتطلبه ممكن يتحقق لأول مرة بدل ما يتعكس (اتخرق ست مرات في الستاك ده).

## CORS

> ⚠️ **و`ready-orders-worker` بقى بيسمح بـ`POST` من v1.2.0** —
> `lookup_orders` بياخد **مصفوفة أكواد** في الجسم (مصفوفة في الـ query
> string بتتقص عند سقف طول الـ URL **في صمت**). ومن غير `POST` في
> `Access-Control-Allow-Methods` الـ preflight بيرفض النداء والواجهة بتقول
> «تعذّر الوصول» على Worker **شغّال**.

**الخمس Workers على Option B** (قايمة صارمة) بنفس الأصل بالظبط
(`https://ecommoda-dev.github.io`) — **والمسار مش جزء من الـ Origin**.
🔴 وWorker الدخول بيستقبل **PIN**، فمفيش wildcard عليه بأي حال.

> ✅ **وعشان كده الدمج (v1.5.0) محتاجش تعديل CORS ولا حرف.** الأداتين
> المدموجتين كانوا **أصلاً** على نفس الأصل بنفس القايمة الصارمة
> (`ALLOWED_ORIGINS = ['https://ecommoda-dev.github.io']`) وبنفس
> `Access-Control-Allow-Methods: GET, POST, OPTIONS`. صفحة في الهب
> (`/Delivery-COD-Operations-Center/COD-Payment-Center.html`) وصفحة في ريبو الأداة
> (`/COD-Payment-Center/`) **ليهم نفس الـ Origin بالظبط** — المسار مش جزء
> منه.
> ⚠️ ودي **الحاجة الوحيدة** اللي خلّت القرار ③ (الروابط القديمة تفضل
> شغّالة) ممكن من غير أي تعديل في أي Worker.

> ⚠️ **وOption B على أداة قراءة-فقط انحراف موثّق** عن `worker-builder` Step 3
> (اللي بيقول wildcard للقراءة). السبب مكتوب في `index.js` بتاع الأداتين:
> الرد بيحمل **اسم العميل ومحافظته ومبلغ التحصيل**، ومستهلكه **واحد معروف**.

## النشر

### 🔴 قاعدة النشر — ومحدش بيعمل Promote يدوي على الدمج

| المصدر | الأمر اللي بيتشغّل | النتيجة |
|---|---|---|
| **دمج في `main`** | `npx wrangler deploy` (Deploy command) | **مفعّلة على طول ١٠٠٪** — ⛔ **مفيش Promote** |
| **push على فرع** | `npx wrangler versions upload` (Version command) | **نسخة بس** — Promote **بس** لو عايز تجرّبها حيًا قبل الدمج (§13-ج) |
| **إضافة/تدوير سر من الداشبورد** | — | بتعمل **نسخة جديدة مش منشورة** → **Promote إلزامي** (الفخ الأول) |

- 🔴 **الـ Promote آلية معاينة فرع وتفعيل سر — مالهاش أي علاقة بالدمج.**
- ⛔ **وممنوع تتكتب في الملف ده أي جملة عن «النسخة النشطة دلوقتي».** مصدرها
  الوحيد **Version History** على كلاودفلير، ونسخة منها في ملف **بتبوظ مع أول
  دمج بلا أي إشارة** — وساعتها بتتقري حقيقة مش لقطة قديمة.
  ⚠️ **وده حصل في الستاك ده فعلاً:** «الـ deployment النشط لسه `1.1.0` · محتاج
  Promote» اتكتبت في `Ready-Orders` **من جوّه فرع** يوم 16-09-2026 وكانت **صح
  وقتها**، والدمج فعّلها بعدها بدقايق — وفضلت مكتوبة في تلات ملفات، واتقريت
  **تلات مرات في تمريرة v1.9.0** على إنها حالة حاضرة (والحقيقة إن `1.3.0` كان
  منشور من ساعات).
- ✅ **اللي بيتكتب هنا هو القاعدة والقرار** (اللي عمره طويل)، **مش الحالة**
  (اللي بتبوظ مع أول `git push`).

### خطوات الإعداد (مرة واحدة وقت الإنشاء)

```
① GitHub Pages → Settings → Pages → Deploy from a branch → main / (root)
② Workers Builds على الريبو ده → Worker الدخول
③ Build watch paths = index.js + wrangler.toml   ← 🔴 وقت الإنشاء مش بعدين
④ WORKER_SECRET (سر مجموعة delivery_cod_ops) → Promote   ← الفخ الأول: السر بيعمل نسخة مش منشورة
```

### 🔴 `Build watch paths` — الغلطة فيها قاتلة وصامتة

الريبو ده فيه **واجهة وWorker**، يعني من غير التضييق **كل تعديل HTML بينشر
الـ Worker تاني** بنفس الكود — ضوضاء في Version History وبناءات متحرقة.

```
Include paths : index.js        ← chip منفصل
                wrangler.toml   ← chip منفصل
Exclude paths : (فاضي — النص الرمادي placeholder مش قيمة)
```

- ⛔ **الحقل chips مش نص.** الاتنين في سطر واحد بمسافة = **chip واحد** باسم
  مش موجود، والنتيجة: **مفيش أي push هيبني تاني — ولا خطأ ولا رسالة ولا أثر،
  والـ Worker بيتجمّد على نسخته للأبد.** اكتب كل واحد لوحده + Enter، واتأكد
  بعينك إنهم صندوقين منفصلين.
- ⚠️ **والتحقق إجباري في الاتجاهين:** تعديل HTML **مايبنيش** من `main`،
  وتعديل `index.js` **يبني**. الاتجاه الواحد شكله زي «الـ paths باظت» بالظبط.
- ⚠️ **وأي ملف جديد يعتمد عليه الـ Worker لازم يتضاف للـ paths** — وإلا يفضل
  على نسخة قديمة **بلا أي رسالة**.
- ℹ️ والـ watch paths **مابتتطبّقش على بناء الفروع** — الفرع بيبني على أي
  تعديل. ده متوقع مش عطل. (`ecommoda-tool-migration-playbook` §13-ب)

### السر

> 🔴 **مجموعة `delivery_cod_ops` — مستقلة عن محطة المخزن** (قرار أحمد
> 15-09-2026). تلات Workers (الدخول + الطابورين) + الهب كمستهلك رابع.
> ✅ **المكسب:** تسريب من جهاز (لقطة شاشة · جهاز مسروق) بيمسّ محطة واحدة بس.
> ⚠️ **وشرطه لازم يتقال:** الهبين على **نفس الـ origin** و`localStorage`
> مشترك بين المسارات — فالعزل حقيقي **بس لو أجهزة الشحن غير أجهزة المخزن**.
> الجهاز اللي بيفتح الاتنين بيبقى شايل السرّين.
> ⚠️ **والثمن:** الجهاز اللي عليه المخزن **مش** مضبوط هنا تلقائيًا — السر
> الجديد بيتلزق مرة واحدة على كل جهاز.

⚠️ **وGitHub Pages بتنشر الـ root كله** (§5.3)، يعني `index.js` و`wrangler.toml`
بيبقوا ملفات مفتوحة على رابط الواجهة. **مش مشكلة عمليًا** — الريبوهات Public
أصلاً (قرار ٢) فالكود مكشوف على GitHub على أي حال، والأسرار ممنوعة في الريبو
بقاعدة مستقلة. بس لازم يتقال عشان محدش يتفاجئ.

| | مطلوب من الريبو ده؟ |
|---|---|
| Worker الدخول · `wrangler.toml` · ربط Builds · Promote | ✅ **أيوه — من هنا** |
| Workers الطابورين · تعديل CORS بتاعهم | ❌ — في ريبوهاتهم |

## 🔴 الصفحتان **متولّدتان** — `docs/build-pages.py`

`ready-orders.html` و`shipped-orders.html` **متطابقتان بالحرف** فيما عدا خمس
معاملات (الحالة · الـ endpoint · الكاش · العمود الزيادة ونصوصه).

> 🔴 **ومن v1.1.0 بقى فيه معامل تاني للأعمدة — `addrHead`/`addrCell`/`addrFn`.**
> عمود «العنوان» في **صفحة الجاهز بس**، لأن `ready-orders-worker` v1.1.0 هو
> اللي بيرجّع `address1`/`address2` و`shipped-orders-worker` **لسه مابيرجّعهمش**.
> ⛔ **وممنوع يتضاف هناك قبل الـ Worker** — عمود بيقول `—` على كل صف بيتقري
> **عطل في الشاشة** مش «الحقل مش موجود». لما الـ Worker يرجّعه، التعديل
> **سطرين معامل** في المولّد وبس.
> ⚠️ ونفس المبدأ على `aboutCols`: بنود «الأعمدة» في نافذة «عن الأداة» بقت
> **لكل صفحة بتاعها** — بند بيوصف عمود مش موجود في الصفحة أسوأ من مفيش بند.

> 🔴 **ومن v1.6.0 معامل تالت للأعمدة — `noteHead`/`noteCell`/`noteFn`.**
> عمود «ملحوظات» في **صفحة الجاهز بس** لنفس السبب بالحرف: `note` بيرجع من
> `ready-orders-worker` **v1.3.0** و`shipped-orders-worker` مابيرجّعهوش.
> ⛔ **وممنوع يتضاف هناك قبل الـ Worker** — وفيه بند في `queues-check.mjs`
> بيقفلها. لما الـ Worker يرجّعه، التعديل **تلات معاملات** وبس.

> 🔴 **ومن v1.3.0 بقى فيه معاملات تاب الجرد — `AUDIT_CSS`/`AUDIT_TABS`/
> `AUDIT_LIB`/`AUDIT_VIEW`/`AUDIT_JS`/`AUDIT_ABOUT` + `apiFns` + `clItems`.**
> كلهم **فاضيين في صفحة المشحون**، والنتيجة إن الصفحة دي ما اتغيّر فيها غير
> غلاف `#viewQueue` وكتلة السجل — **صفر خطر على بنودها**.
> ⚠️ و`apiFns` مقصودة: `apiPost` بيتحقن **في الصفحة اللي بتناديه بس** —
> دالة بلا مستهلك = كود ميت.
> ⚠️ و`AUDIT_VIEW` بتنادي `addressCell` و`whereaboutsCell` (معاملان في
> صفحة الجاهز) — فتشغيل الجرد في صفحة تانية محتاج الدالتين معاه.

```bash
python3 docs/build-pages.py
node docs/rules-check.mjs && node docs/queues-check.mjs
```

- 🔴 **ليه مولّد أصلاً:** الصفحتين بيعملوا نفس الشغل بالظبط، ونسختين بتتعدّلوا
  بالإيد **بيفترقوا مع أول تعديل** — واتصلح في واحدة وفضل مكسور في التانية
  **لشهور** (درس R1، وده بالظبط اللي حصل بين `Ready-to-Pack` و
  `Orders-Packing-Checker`). المولّد بيحوّل «افتكر تعدّل في الاتنين» لـ«مستحيل
  تعدّل في واحدة».
- ⛔ **وممنوع تعديل الـ HTML مباشرةً** — التعديل بيضيع **في صمت** مع أول تشغيل،
  وفيه بانر في أول كل ملف بيقول كده.
- ⚠️ **والمولّد بيدّي كل صفحة الدالة بتاعتها بس** (`whereaboutsCell` للجاهز ·
  `trackingCell` للمشحون) — دالة بلا مستهلك = كود ميت، مش «احتياط».

## 🔴 فحص CSS بـ parser — إلزامي، وgrep مش بديل عنه

```bash
npm i postcss --no-save
node docs/css-check.js shared/shell.css index.html ready-orders.html shipped-orders.html \
                       Order-Status-Updater.html COD-Payment-Center.html Partial-Delivery.html
```

> 🔴 **والصفحتين المدموجتين لازم يدخلوا في نفس النداء** — كتلة التوكنز
> بتاعتهم اتشالت لصالح الـ shell، فلو واحدة منهم اتنست الفحص **مش هيمسك**
> الاسم البديل المهجور (`--text-sub` · `--mono` · `--accent-b`) اللي كان
> جوّه كتلتهم القديمة. المتغيّر غير المعرّف بيتجاهله المتصفح **في صمت**:
> الخط بيرجع للافتراضي واللون بيرجع أسود، بلا أي خطأ في الكونسول.
> ✅ ودي اتمسكت فعلاً وقت الدمج — ٣٠ استخدام في الصفحتين.

> ⚠️ **لازم `shared/shell.css` يبقى أول وسيط** — التوكنز معرّفة هناك، والفحص
> بيجمع التعريفات من كل الملفات اللي بتتمرر له. من غيره الفحص بيقول إن ٦١
> متغيّر «مستخدم ومش معرّف» وهو غلط.

> 🔴 **القاعدة:** ممنوع أي علامة تعليق جوّه تعليق CSS. تعليقات CSS
> **مابتتداخلش** — أول `*/` بيقفل الكتلة بدري، وباقي النص بيتقرا CSS،
> والبارسر بيبلع **كتلة التوكنز كلها** جوّه selector غلط. الصفحة بتفضل شغّالة
> وشكلها مكسور **من غير أي خطأ في الكونسول**. ده حصل فعلاً في هب المخزن v1.0.0.

## 🔴 فحص متصفح فعلي — ٢٧٠ بند

```bash
npm i playwright postcss --no-save
node docs/queues-check.mjs
```

> ⚠️ **ملف واحد للتلات صفحات — عكس قرار هب المخزن (خمس ملفات)، وده مقصود.**
> التلاتة بيقروا **نفس شكل الرد بالحرف** وبيشتقّوا منه بـ**نفس الدوال**. وWorker
> وهمي واحد هنا **جزء من البند نفسه**: أهم بند في الملف إن **الرقم في الرئيسية
> == عدد صفوف الصفحة**، ومستحيل تقيسه لو كل صفحة بتشوف بيانات مختلفة.

> 🔴 **وبقى ٢٧٠ بند في v1.9.0** — ١١ بند جديد، وكلهم بيقروا **الشاشة
> الفعلية**:
> · 🔴 **عمود «العميل»** (٨ بنود): اسم خماسي طويل بيترسم على **سطرين
>   بالكتير** (البند بيقسم ارتفاع العنصر على `line-height` — مش بيقرا
>   `-webkit-line-clamp`) · والبنط **أصغر من بنط الجدول** · والاسم
>   **جوّه عرض الخلية** · و`title` فيه الاسم **كامل** · ⛔ **والعرض ما
>   اتغيّرش** (العميل لسه أضيق من عمود التاريخ و«العنوان»/«ملحوظات» زي ما
>   هما) · والبحث بيلاقيه **بآخر الاسم** · وفي **الصفحتين**.
> · 🔴 **أرضية التاريخ** (٣ بنود): الشيب مكتوب جنب الرقم بصيغة الجدول
>   (`dd/mm/yyyy`) في **الصفحتين** · ⛔ **وبيختفي خالص** لما الرد ما
>   يرجّعش `minCreatedDay` (Worker أقدم) — وده البند اللي بيثبت إن
>   التاريخ **جاي من الرد** مش مكتوب في الصفحة.

> 🔴 **وكان ٢٥٩ بند في v1.8.0** — ٢٦ بند، وكلهم بيقروا **الشاشة
> الفعلية**:
> · 🔴 **مربعات «موقع الشحنة»**: الأربعة بترتيبهم · وليبل كل مربع ==
>   ليبل القايمة · **ومجموعهم == الرقم الكبير** · 🔴 **ورقم كل مربع ==
>   عدد الصفوف اللي بيفتحها** (ضغطة على تلات مربعات) · والمربع بيكتب في
>   الفلتر **وفي الاتجاه التاني كمان** · والأعداد **ما بتتغيّرش بعد
>   الفلترة** · والمربع بصفر **بيفضل ظاهر** (على طابور ناقصة منه القيمة)
>   · ⛔ **وصفحة المشحون مالهاش الصف ده**.
> · 🔴 **خلية التاريخ سطران** — البند بيقرا `getClientRects().length` على
>   **كل** خلية تاريخ في **الصفحتين**: سطر واحد للتاريخ · والأيقونة
>   **لسه معاه** · والبادج تحته · والسطر **جوّه العمود** (مش بيزقّ عرضه).
> · **وبند الفاضي في قايمة «موقع الشحنة»** بقى `—` بالحرف · ⛔ **وبلا
>   «مش مسجّل»**.

> 🔴 **وكان ٢٣٣ بند في v1.7.0** — ٢٥ بند، وكلهم بيقروا **الشاشة
> الفعلية**:
> · **قايمة «موقع الشحنة»**: الليبلات بنص البادج بالحرف · ⛔ **والقيمة
>   الخام مش معروضة** · والفاضي باسمه · ونص الخلية **نفسه بالحرف** ·
>   والاختيار **بيفلتر فعلاً** (الفلترة على القيمة الخام) · والشيب وزرار
>   الفلتر بنفس الليبل.
> · **فلتر «التغليف»**: **قيمتان بالظبط** · و«لم يتم التغليف» بيطلّع الصف
>   اللي `packedAt` بتاعه فاضي **بالظبط** · وعمود التاريخ على نفس الصف
>   بيقول `—` · والاتنين بيكمّلوا الطابور.
> · 🔴 **مقاسات الأعمدة** — البند بيقرا **العرض المحسوب**: عمودا التاريخ
>   **متساويان** · «العميل» أضيق من عمود التاريخ · والزيادة في «العنوان»
>   و«ملحوظات».
> · **اسم التاب** «الأوردرات» **ومفيش «الطابور»**.
> · 🔴 **فلاتر تاب الجرد**: فلترين بأسمائهم وبترتيبهم · والمندوب
>   **مجموعات** مش أسماء · وبلا اختيار = الكل · والاختيار بيغيّر النطاق
>   **قبل البدء** · 🔴 **وفلتر تاب «الأوردرات» مالوش أي أثر على النطاق** ·
>   والفلاتر **بتتقفل** بعد البدء (وبحارس تاني على نداء مباشر) · وسطر
>   القفل بيبان · والفلتر بالنص متسجّل للتصدير · وبترجع مقفولة بعد
>   `refresh` · و«جرد جديد» بيفكّها.

> 🔴 **وكان ٢٠٨ بند في v1.6.0** — ١٢ بند جديد على تمريرة الطابور، وكلهم
> بيقروا **الشاشة الفعلية**:
> · **عمود «ملحوظات»**: النص بالحرف · جوّه `.note-txt` (اللون بيتقرا من
>   **وجود العنصر** مش من اسم كلاس في الكود) · الخلية `.note-cell` ·
>   🔴 **مسافات بس == `—`** · والصف بلا `note` خالص == `—` · والبحث
>   بيدوّر في نص الملحوظة.
> · **«موقع الشحنة»**: `Office` → أخضر «✅ في المكتب» · `Warehouse`
>   و`Courier` → أحمر بـ⚠ **وبالقيمة بالحرف** · 🔴 **والفاضي بلا أي بادج**.
> · **بادج الأيام تحت التاريخ**: البند بيقرا `display` **المحسوب** من
>   `.cell-date` في العمودين — لفّ سطر بالصدفة مابيعديش.
> · ⛔ **وصفحة المشحون بلا عمود «ملحوظات»** — بند بيمنع إضافته «بالقياس»
>   قبل ما `shipped-orders-worker` يرجّع الحقل.

> 🔴 **وكان ١٩٦ بند في v1.4.1** — ٨ بنود جديدة على الاستعلام التلقائي:
> · 🔴 **`lookup_orders` اتنادى تلقائيًا مع «إنهاء الجرد»** — والبند
>   **مابيضغطش الزرار خالص**: الضغط كان هيخفي بالظبط اللي بيقيسه.
> · **والسبب بيقول الحالة الحقيقية بالحرف** من غير تدخّل · و«اضغط استعلام»
>   مابقاش مكتوب على صف اتستعلم فعلاً · والزرار مش ظاهر.
> · 🔴 **وكتلة كاملة لفشل النداء التلقائي** (الـ Worker بيرجّع `404`):
>   النداء اتبعت · الرسالة **بتسمّي `1.2.0`** · الزرار ظاهر **بنص «إعادة
>   محاولة»** · «لسه ما استعلمناش» ≠ «مش موجود على شوبيفاي» · والفعل على
>   الصف بيقول «إعادة المحاولة» · والضغط عليه بيجيب الحالة.

> 🔴 **وكان ١٨٨ بند في v1.4.0** — ١٩ بند على تمريرة شكل الجرد، وكلهم
> بيقروا **الشاشة الفعلية**:
> · **أسماء المربعات الأربعة بترتيبها على الشاشة** كمصفوفة واحدة — الاسم
>   لوحده بيعدّي لو مربعان اتبدّلوا مكانهم، والترتيب لوحده بيعدّي لو الاسم
>   اتغيّر.
> · 🔴 **العنوان بيقول النطاق بمجموعاته** قبل البدء وبعده، و**بمجموعته هي
>   بس** لما الجرد يبدأ على فلتر · والفلتر الكامل بالنص **لسه متسجّل
>   للتصدير**.
> · ⚠️ **والنصوص اللي اتشالت مش موجودة** — بند بيمنع رجوعها «بالقياس».
> · 🔴 **بعد الإنهاء التلاتة مطوية** · ضغطة مربع بتفتح قسمه **وبتطوي الباقي**
>   · «إجمالي الأوردرات» بيفتح التلاتة · والمربع المختار **بياخد إطار**.
> · ⛔ **والضغطة وسط الجرد مابتفتحش أي قسم**.
> · 🔴 **أعمدة التلات أقسام == أعمدة جدول الطابور بالحرف وبالترتيب** (بند
>   لكل قسم، بيقارن المصفوفة كلها) · و**خلايا الصف == عدد الأعمدة**.
> · 🔴 **و«موجودة خطأ» جدول كمان** والسبب في **صف بـ`colspan`** تحت الصف.
> · ⚠️ **وعناوين الأقسام بلا الجمل الشارحة وبلا عدّاد** — بند على المصفوفة
>   كلها وبند على غياب `.aud-sec-n`.

> 🔴 **وكان ١٦٩ بند في v1.3.0** — ٥٢ بند على تاب «جرد المكتب» (§⑨)،
> وكلهم بيشغّلوا **الدورة الفعلية على الشاشة** مش بيقروا الكود:
> · **العدّاد `—` مش `0` قبل البدء** · **السكانر معطّل** قبل البدء
>   **وحارس تاني على `audScan` نفسها** (نداء مباشر مابيتحسبش).
> · سكان بالـ Order ID → «مظبوط» · **نفس الأوردر بالاسم مابيتعدّش تاني** ·
>   **كود أقل من ٤ أرقام مابيتحسبش** · كود بره القسم → «موجودة خطأ».
> · 🔴 **مربع «مفقودة» مخفي وسط الجرد** وبيظهر بعد الإنهاء — و«أوردرات
>   متبقية» بتختفي: نفس الرقم واسمين.
> · **صفوف كل قسم == الرقم اللي المربع بيقوله** · والصف المعلّم **في قايمة
>   المفقود زي أي صف** وعلامته لسه بتفتح نافذة السبب والفعل.
> · 🔴 **النطاق = المعروض بعد الفلتر** (اتغيّر في v1.7.0) · **ولقطة**: مسح الفلتر بعد البدء
>   مابيغيّرش النطاق · والفلتر المثبَّت **مكتوب بالنص**.
> · 🔴 **`lookup_orders` اتنادى بـ POST والكود في `ids`** · وبعد الاستعلام
>   السبب بيقول الحالة الحقيقية **بالحرف**.
> · 🔴 **الجرد بيرجع بعد `refresh`** بنطاقه وسكاناته وحالته.
> · 🔴 **والتصدير بلا ExcelJS بيقول «المكتبة ما اتحمّلتش»** — مش
>   `ReferenceError` صامت بيسكّت باقي السكربت.
> · ⛔ و**صفحة المشحون مالهاش تاب جرد ولا مربع سكان ولا المكتبة**.

> 🔴 **وكان ١١٧ بند في v1.2.0** — ١٦ بند على القسم الموحّد، وكلهم
> بيقروا **الشاشة الفعلية**: دورة الترتيب التلاتية (تصاعدي بالمقارنة الفعلية
> للقيم · تنازلي == معكوس التصاعدي · التالتة بترجّع للافتراضي) · **السهم
> مايظهرش قبل أول ضغطة ويختفي بعد التالتة** · **الترتيب بيفضل بعد «مسح كل
> الفلاتر»** (عمليتان منفصلتان) · **الصف اللي ما اتغلّفش بيروح آخر الترتيب
> التصاعدي** · «النتائج» == المعروض · **المربع بيكتب في فلتر المندوب** ·
> وتلات حالات أيقونة الفلتر وزرار المسح (§3).

**بيفحص:** الجلسة والتحويل بـ`?next=` · الهيدر و`aria-label` الخروج · تطابق
زرار النسخة مع بادج الـchangelog · **صفوف الجدول == اللي رجع** · **الرقم
الكبير == الصفوف** · **خلايا الصف == أعمدة الهيدر** · الترتيب الأقدم فوق ·
**وقت تغليف S2 لصف S2** · **رقم تتبع S2 لصف S2** · علامة الحقل القديم ·
الصفوف المعلّمة **لسه في الطابور** · نافذة المراجعة بالسبب والفعل · الفلتر
الأحادي و«معروض N من M» · **المربعات بتعدّ من الكامل** · البحث · «مفيش نتيجة»
≠ «فاضي» · بانر الفشل وبانر الاقتطاع **منفصلين** · **«تعذّر» مش `0`** ·
`get_shipped_queue` اتنادى · **حارس الفلفلمنت على S1 بس** · «مستحق التحصيل»
= PENDING بس · المدفوع مقدمًا **بعدده** · **الرقم في الرئيسية == الصفحة** ·
`allSettled` (فشل طابور مايخفيش التاني) · الفحص الذاتي بيسمّي الـ Workers
و**صفر قيمة سر** · `hint` تحت الفاشل بس · حارس النسخة بيسمّي الأداة ·
**الدخول راح لـ Worker الهب** · **`appId` ما اتبعتش** · الـ PIN في الـ body ·
`?next=` الخارجي **بيترفض** · والخروج راح لنفس Worker الدخول **وبلا `appId`**.

> 🔴 **وبقى ١٠١ بند في v1.1.0** — تمريرة الأعمدة ضافت ١٩ بند، وكلهم بيقروا
> **الشاشة الفعلية** مش الكود:
> · **قايمة أعمدة الجدول بترتيبها بالحرف** (بند واحد بيقارن المصفوفة كلها) ·
>   و**«نوع الأوردر» قبل الأخير** كبند مستقل عشان يفضل هو الشرط المسمّى لو
>   اتضاف عمود جديد يوم · وأربع بنود على الأعمدة اللي اتشالت **بالاسم**
>   («القناة» · «عدد القطع» · «الإجمالي» · «عهدة الطرد») — العدّ لوحده
>   بيعدّي لو عمود اتشال وعمود تاني اتضاف مكانه.
> · **خلية العنوان**: `address1`+`address2` مع بعض · **المدينة == المحافظة
>   بتتعرض مرة واحدة** (`Cairo · Cairo` بتتقري غلطة إدخال) · و**«بلا عنوان»
>   بتتقال بالنص** على الصف اللي مالوش عنوان.
> · **خلية العميل بقت الاسم وبس** و**خلية المندوب بقت الاسم وبس** — البندان
>   دول هما اللي بيمسكوا رجوع السطر المكرّر تحتهم.
> · **شكل مربع الفلتر**: البند بيقرا `border-radius` **المحسوب من العنصر
>   نفسه** مش اسم الكلاس — كلاس اتغيّر من غير CSS وراه بيعدّي على أي grep،
>   والمربع بيفضل pill.
> · و**طابور المشحون بلا عمود عنوان** — البند ده بيمنع إن العمود يتضاف هناك
>   قبل ما `shipped-orders-worker` يرجّع الحقل.
> 🔴 **ونسخة الـ Worker الوهمي بقت لكل أداة لوحدها** (`ready` على `1.1.0` ·
> `shipped` على `1.0.0`). رقم واحد للاتنين كان بيولّع «Worker نسخة قديمة»
> على صفحة الجاهز في **كل** بند، فالبنود تفشل **لسبب مالوش علاقة باللي
> بتقيسه**.

> 🔴 **البند اللي الفحص ده مسكه فعلاً وقت البناء:** `qRender()` اللي في
> `finally` كانت **بتدهس** حالة الفشل اللي `catch` كتبها، فالرقم كان بيرجع `—`
> بدل «تعذّر» — يعني الفشل بيبان زي أول تحميل. **الشاشة كانت بتفتح والكونسول
> نضيف**، ومراجعة الكود كانت هتعدّي عليه.

> ⚠️ **`PW_CHROMIUM`** بيسمح بتمرير مسار كروميوم مثبّت مسبقًا لو Playwright
> ما نزّلش نسخته.

## 🔴 فحص عقد الدمج — ٧٨ بند (v1.5.0)

```bash
npm i playwright --no-save
node docs/tools-check.mjs
```

> 🔴 **ليه ملف رابع؟** التلاتة اللي قبله بيقيسوا حاجات تانية خالص:
> · `rules-check.mjs` → منطق الطوابير والجرد على **مدخلات باظة**.
> · `queues-check.mjs` → الطابورين والرئيسية على **بيانات كاملة سليمة**.
> · `css-check.js` → التوكنز **بالـ parser**.
> الملف ده بيقيس حاجة واحدة بس: إن الأداتين بقوا **فعلاً** جوّه الهب.
> ⛔ **وهو مش بديل عن فحص الأداتين نفسهم** — منطق التحصيل ومنطق تحديث
> الحالة **ما اتلمسوش**، فحصهم في ريبوهاتهم. البنود هنا كلها عن **الإطار**.

**بيفحص — وكله من الشاشة الفعلية ما عدا القسم ⓪:**

| القسم | البنود |
|---|---|
| ⓪ ملفات | 🔴 **صفر تصادم `const`/`let` مع الـ shell** · صفر كتلة توكنز (`--container-max` بس) · 🔴 **الصفحة متولّدة فعلاً**: تشغيل المولّد بيدّي نفس البايتات |
| ① الجلسة | بلا جلسة → تحويل بـ`?next=` · ⛔ **وصفر نداء Worker قبل التحويل** · صفر شاشة دخول · ولا كيباد PIN |
| ② السر | كل نداء بسر الهب مش السر القديم · وعلى Worker الأداة نفسها · ⛔ وبلا سر = **صفر نداء** مش نداء بـ`Bearer ` فاضي |
| ③ الشِل | العنوان · 🏠 الرئيسية · اسم الجلسة + `aria-label` · النسخة == بادج الـchangelog == نسخة الهب · مودال إعدادات **واحد** · توست واحد · **و`.mtb-badge` ليه ستايل محسوب** |
| ④ الرئيسية | كارتين بيفتحوا الصفحتين · بالاسم المطابق · ⛔ **بلا عدّاد** · ⛔ **ومش رابط خارجي** |
| ⑤ المنطق | 🔴 **`apiPostLegacy` لسه بيبعت `action` في الجسم** · التوقيت +٣ قبل 29-10 و**+٢ بعدها** · 📅 مرة واحدة · حارس `Invalid Date` · 🔴 **واختيار حالة `Returned` بيكمّل لآخره**: صفر خطأ · `reason_values` **اتنادى فعلاً** · والكاش مابقاش `null` |
| ⑥ الرسايل | الفشل **بيسمّي الأداة** — الهب بينادي خمس Workers |
| ⑦ التصدير | 🔴 **CDN محجوب فعلاً** → الأربع أزرار بتقول «مكتبة التصدير ما اتحمّلتش» |

> 🔴 **أخطر بند في الملف هو الأول** — `shell.js` وسكربت الصفحة في **نفس
> النطاق العام**، وإعادة تعريف `const` بنفس الاسم بترمي «Identifier has
> already been declared» و**بتقتل سكربت الصفحة كله**: الصفحة بتفتح، الشِل
> شغّال، وكل زرار في الأداة **مابيعملش حاجة**.
> ⚠️ **والنمط بيقرا عمود صفر بالظبط** — `const` جوّه دالة نطاقها الدالة
> فمافيش تصادم منها، ونمط متساهل كان بيطلّع عشرات الأسماء الوهمية
> (`d` · `rows` · `btn`) والبند يبقى **ضوضاء بيتتجاهل**.

> 🔴 **وتلات بنود جداد (17-09-2026) — اتكتبوا بعد عطل حقيقي في النسخة
> المنشورة، والعطل ده عدّى على الـ٦٥ بند كلهم.** `focusScan()` في
> `order-status.html` كانت لسه بتسأل عن `#loginOverlay` — عنصر **شاشة
> الدخول** اللي المولّد بيشيلها. `getElementById` بترجّع `null`،
> و`.classList` بترمي **وتقطع الدالة اللي نادتها**.
> ⛔ **وأخطر نداء ليها `selectTargetLabel()`**: بترسم الجدول الأول (فكل
> صف بياخد «⏳ جاري تحميل الأسباب...» و`disabled`)، وبترمي **قبل سطر
> واحد** من `ensureReasonValues()`. فنداء `reason_values` **عمره ما
> بيتبعت**، والكاش بيفضل `null` **للأبد** — دي نقطة النداء الوحيدة في
> الصفحة كلها. والسبب **إلزامي**، يعني `Cancelled` و`Returned` كانوا
> **مقفولين بالكامل** من نسخة المركز، **وبلا أي رسالة للموظف**.
> ⚠️ **وليه الـ٦٥ بند عدّوا عليه:** بند «صفر خطأ في الكونسول» بيقيس
> **التحميل**، والرمي بيحصل عند **أول ضغطة** على حالة. فالبنود الجداد
> **بتضغط الزرار فعلاً** وبتقيس تلات حاجات: صفر خطأ · `reason_values`
> اتنادى · والكاش مابقاش `null`.
> ✅ **والإصلاح في المولّد مش في الملف المتولّد** — والحارس التاني تحت
> (`check_dom_ids`) بيقفل العيلة كلها مش الحالة دي بس.

> ⚠️ **وبند «الصفحة متولّدة» بيتخطّى** لو الريبوهات الأصلية مش جنب الهب —
> مش كل بيئة فيها التلاتة. بيقول كده بالنص (`⏭️`) مش بيعدّي في صمت.

## 🔴 فحص منطق — ٩٠ بند

```bash
node docs/rules-check.mjs        # بلا أي تنصيب — منطق بحت
```

> 🔴 **ليه ملف تالت غير فحص المتصفح؟** الاتنين بيمسكوا عيلتين مختلفتين:
> · `queues-check.mjs` بيشغّل الصفحة على **بيانات كاملة سليمة** — بيمسك
>   **الربط** (الرقم في الرئيسية == القايمة في الصفحة · الصف المعلّم لسه في
>   الطابور · الفلتر بيفلتر).
> · `rules-check.mjs` بيدّي للدوال **مدخلات باظة وحدّية** — `null` · تاريخ
>   غلط · قيمة بره القايمة · **تحوّل التوقيت الشتوي**. دي حالات **مستحيل
>   تتزرع في بيانات وهمية معقولة**، وهي بالظبط اللي بتنتج «رقم غلط شكله سليم».

> 🔴 **وتلات عطلين حقيقيين اتمسكوا هنا وقت البناء — وكلهم عدّوا على فحص
> المتصفح** (لأن بياناته الوهمية كلها كاملة):
> **①** `new Date(null)` بيرجّع **1970-01-01 صالح** مش `Invalid Date` — فأوردر
>   مالوش تاريخ كان بياخد بادج أحمر بيقول **«متأخر ٢٠٬٧٠٠ يوم»**. الفحص بـ
>   `isNaN` لوحده مابيمسكهوش.
> **②** `Number(null)` بيرجّع **0** — فخانة فلوس مالهاش قيمة كانت بتقول
>   **«0 ج»** بدل «—»: «مفيش فلوس» على أوردر إحنا مش عارفين قيمته.
> **③** ونفس السبب في `dcoCod`: أوردر `PENDING` **مالوش مبلغ** كان بيتعدّ
>   **أوردر مستحق بصفر جنيه** — عدّاد الأوردرات بيكبر والمجموع مايزيدش،
>   فالرقم بيقلّ **من غير أي إشارة**.

> 🔴 **وبقى ٩٠ بند في v1.3.0** — ٣٨ بند جديد على §AUDIT-RULES: التطبيع
> (ID · اسم · `gid` · رموز السكانر · `null` · كود بلا أرقام) · المطابقة
> (**الـ ID الأول** · `55001` بيطابق `#55001` · رقم التتبع بالتساوي الكامل ·
> `123` **مابيطابقش** `1234567` · صف `null` جوّه القايمة) · التلات أقسام
> (نفس الأوردر بصيغتين = **مرة واحدة** · الصف بلا `orderId` مابيدخلش النطاق ·
> `orderId` رقمي بيطابق النصّي) · والسبب (**كل سبب بيقول label و detail و
> action** · الإلغاء بيغلب · «لسه ما استعلمناش» ≠ «مش موجود»).

**وبيغطي كمان:** إن الأداة **مش في نطاق بند 29-10-2026** (نفس الكود بيدّي
إزاحة صح قبل التحويل الشتوي وبعده) · فرق الأيام **التقويمي** مش ٢٤ ساعة ·
الجمع العربي · التسع علامات بأكوادها · إن كل علامة بتقول **القيمة الغلط
والفعل** · إن `dcoQueueRows` **مابتفلترش** · إن مربع «محتاجة مراجعة» مابيظهرش
وهو صفر · ومقارنة النسخة **الرقمية** (`1.10 > 1.9`).

## مسائل مفتوحة

- 🔴 **تدوير `WORKER_SECRET` على Workers الأدوات المدموجة — حاجز التشغيل
  الوحيد لـv1.5.0 وv1.10.0.** قرار أحمد ② كان **ضمّ الـ Workers لمجموعة
  `delivery_cod_ops`** بدل ما الهب يخزّن سر لكل أداة. الخطوة يدوية في
  داشبورد كلاودفلير:

  | الـ Worker | الريبو | المطلوب |
  |---|---|---|
  | `order-status-updater-worker` | `Order-Status-Updater` | `WORKER_SECRET` ← قيمة مجموعة `delivery_cod_ops` |
  | `cod-payment-center-worker` | `COD-Payment-Center` | نفس القيمة |
  | `partial-delivery-worker` | `Partial-Delivery` | نفس القيمة (كانت سر مستقل تمامًا قبل v1.10.0) |

  ⛔ **قبل الخطوة دي `Order-Status-Updater.html`/`COD-Payment-Center.html`/
  `Partial-Delivery.html` بيرجّعوا `401` على كل نداء.**
  ✅ **والفشل مش صامت:** الرسالة بتسمّي الأداة، و🩺 فحص النظام من الشاشة
  الرئيسية بيعرض الستة Workers ويقول أنهي واحد رافض. وحارس النسخة **مابيولّعش
  تحذير كاذب** — `401` معناه «مش واصل» مش «نسخة قديمة».
  ⚠️ **وثمن مُعلَن على order-status/cod-payment بس:** الرابط القديم بتاعهم
  بينادي **نفس الـ Worker**، فالموظف اللي بيفتحه لازم يلزق القيمة الجديدة في
  إعداداته هو كمان. **والتسليم الجزئي مالوش الثمن ده** — مفيش رابط قديم
  خالص بعد v1.10.0.

- 🔴 **نسختين من واجهة order-status/cod-payment على الهوا — قرار أحمد ③،
  والخطر معروف ومُعلَن.** ده درس R1 حرفيًا. المخفّف إن نسخة الهب **متولّدة**
  (`docs/port-standalone.py`) وفيه بند بيقارن البايتات، بس **المولّد
  مابيتشغّلش لوحده**: أي إصلاح في الريبو الأصلي محتاج تشغيلة ودفعة هنا.
  ⛔ **والاتجاه واحد** — تعديل في نسخة الهب بالإيد **بيضيع**، والفحص بيمسكه.
  ⚠️ **والتسليم الجزئي خارج البند ده تمامًا من v1.10.0** — مفيش نسخة تانية
  ولا مولّد، فمفيش خطر افتراق أصلاً (راجع قسم «الأدوات المدموجة» فوق).
  ⚠️ **ولو اتقرر يوم إن الروابط القديمة تتحوّل للهب**، ده بيقفل البند ده
  بالكامل والمولّد بيبقى خطوة نقل مرة واحدة بس.

- ✅ **إعداد الـ Workers التلاتة — اتعمل ومتأكَّد بالعين (آخر تأكيد 17-09-2026).**
  التلاتة متعملين بالأسماء الصح · Builds مربوط على `main` · `Build watch paths`
  = `wrangler.toml` + `index.js` **chip لكل واحد** · والأسرار متحطّة.
  ⛔ **والجدول اللي كان هنا بعمود «النسخة النشطة» اتشال** — ده بالظبط نوع
  الادعاء اللي بيبوظ مع أول دمج (القاعدة في قسم «النشر» فوق).
  ✅ **والدليل العملي الباقي:** `ready-orders.html` اتفتحت حيًا يوم 16-09-2026
  وطلّعت **١٢٦ أوردر بعناوين حقيقية** — الدخول والسر وOAuth والطابور كلهم
  شغّالين من الأول للآخر.
  ⚠️ **ومفيش نسخة مستقلة لأي أداة فيهم** — لو Worker وقع، مفيش نقطة رجوع.
  🔴 **وWorker الدخول أخطرهم لو وقع:** الطابور بيقع لوحده، لكن الدخول بيقفل
  المحطة كلها.

- 🟢 **نشر الطابورين مش بند مفتوح** — الدمج في `main` بينشر ويفعّل لوحده
  (القاعدة في قسم «النشر» فوق)، وحارس النسخة في الصفحة هو اللي بيقول لو
  Worker اتأخر فعلاً. ⛔ وأي بند هنا بيقول «النسخة الفلانية لسه محتاجة
  Promote» **بند بايت** — الحالة مكانها Version History على كلاودفلير.

- 🟡 **ExcelJS من CDN — أول مكتبة خارجية في الهب.** الاستخدام محروس، والفشل
  بيقول «المكتبة ما اتحمّلتش» (وفيه بند بيقفله). بس البند اللي لسه مفتوح:
  **مفيش نسخة محلية** — CDN محجوب على شبكة المحطة معناه إن التصدير بيقف
  (والجرد نفسه بيفضل شغّال على الشاشة). القرار لأحمد: نسيبها CDN أو ننزّل
  الملف في الريبو.

- 🟡 **الجرد في `sessionStorage` بس — قفل التاب بيمسحه.** ده ثمن مُعلَن
  (مكتوب في «عن الأداة»)، وسببه إن السر هو **الحاجة الوحيدة** في
  `localStorage` في الريبو كله (#28 · #39). لو اتطلب جرد يعيش عبر التابات،
  ده **قرار منفصل** لأنه بيفتح تخزين دائم تاني على نفس الـ origin المشترك مع
  هب المخزن.

- 🟡 **مفيش سجل جرد في D1 — ولا أي أثر بعد قفل التاب.** يعني سؤال «مين جرد
  امبارح وطلّع كام مفقود؟» **بلا إجابة** غير من ملف XLSX الموظف صدّره.
  ⛔ ولو اتقرر يتسجّل: الـ binding يترجع في `wrangler.toml`، و`tool`/`type`
  (`delivery_cod_ops_center` · `audit_office`؟) **يتسجّلوا في
  `ecommoda-constants` §7 قبل أول `writeLog`** (Rule 7) — والقاعدة دي اتخرقت
  ست مرات في الستاك ده.

- 🟡 **جرد «المشحون» مش مطروح** — أوردر `Shipped` خرج من المكتب بالتعريف.
  ⛔ ولو اتطلب، لازم الأول يتقرر **إيه المفروض يكون موجود** في الطابور ده
  (قرار أحمد مش قرار كود)، وفيه بند في فحص المتصفح بيمنع إن التاب يتحقن
  هناك «بالقياس».

- 🔴 **تسجيل مجموعة `delivery_cod_ops` في `ecommoda-constants` →
  `references/secret-groups.md`** (قاعدة ٢) — **مجموعة جديدة بالكامل**، مش
  انضمام لمجموعة قايمة. الأعضاء **من v1.10.0**: **ستة Workers** (الدخول ·
  `ready-orders` · `shipped-orders` · `order-status-updater` ·
  `cod-payment-center` · `partial-delivery`) + الهب كمستهلك سابع، ومفتاح
  `localStorage` = `delivery_cod_ops_worker_secret`.
  ⚠️ **ولازم يتسجّل معاه إن `order_status_worker_secret` و
  `cod_payment_center_worker_secret` و`partial_delivery_worker_secret` بقوا
  مهجورين** — المفتاحين الأولانيين ما اتمسحوش من متصفحات الموظفين وواجهتا
  الريبوهين القديمة لسه بتقراهم؛ والتالت (`partial_delivery_worker_secret`)
  **مهجور تمامًا** — الريبو بتاعه بقى Worker وبس ومفيش واجهة تقراه خالص.
  ⚠️ **عضو غير مسجّل = إجراء التدوير بيتكسر بصمت** — بتغيّر بعضهم وتنسى
  الباقي، والنتيجة `401` في أداة محدش هيعرف سببها.
  ✅ و`?action=diag` بيعرض **بصمة السر القصيرة** في الستة، فالتأكد من
  التوحيد بيتم من غير كشف القيمة.

- 🔴 **تسجيل `delivery_cod_ops_center` في `ecommoda-constants` §7** بقيم
  `login` · `logout` — **صف `tool` جديد بالكامل**. Rule 7 بتقول التسجيل **قبل**
  أول `writeLog`، والقاعدة دي اتخرقت ست مرات في الستاك ده وكل مرة الادعاء كان
  موجود في `CLAUDE.md`. **التحقق الوحيد المقبول `grep` على المهارة نفسها.**
  ⚠️ ودي فرصة نادرة: الهب **لسه ما كتبش ولا صف**، فالترتيب اللي Rule 7 بتطلبه
  **ممكن يتحقق** بدل ما يتعكس.

- 🟡 **عمود «العنوان» في طابور «مشحون» — مستني الـ Worker (جديد v1.1.0).**
  `ready-orders-worker` v1.1.0 بيرجّع `address1`/`address2`،
  و`shipped-orders-worker` **لسه مابيرجّعهمش**، فالعمود في صفحة الجاهز بس.
  ⛔ **وممنوع يتضاف هناك قبل الـ Worker** — عمود بيقول `—` على كل صف بيتقري
  **عطل في الشاشة** مش «الحقل مش موجود». وفيه بند في `docs/queues-check.mjs`
  بيقفل ده.
  **اللي مطلوب لما يتقرر:** `shippingAddress { name address1 address2 city
  province }` في `QUEUE_QUERY` + السطرين في `shapeOrder` (صفر زيادة في تكلفة
  الاستعلام — سكالر جوّه كائن متحدّد أصلاً)، وبعدها **سطرين معامل** في
  `docs/build-pages.py` (`addrHead`/`addrCell`/`addrFn` + بند `aboutCols`)
  و`shipped.min` يترفع في نفس التسليم.

- 🟡 **`In-Return` مش في طابور «مشحون» — قرار مستني أحمد.** قاعدة ١٢ في
  `ecommoda-order-lifecycle` بتقول `In-Return` تتعامل **زي `Shipped` بالظبط في
  العدّ والفلوس، في الماكينتين**، والمواصفات اللي الأداة اتبنت عليها بتقول
  **`Shipped` بالحرف**.
  ⛔ **والتعارض ده مايتحسمش من الكود:** إدخالها بيكبّر الرقم اللي اتطلب، وشيلها
  من القاعدة ١٢ بيكسر قاعدة.
  ⚠️ **واللي بيمنع ده من إنه يبقى غياب صامت:** عنوان الطابور وشرحه في «عن
  الأداة» بيقولوا `Shipped` بالنص. لو اتقرر دخولها، التعديل مكانه **الاستعلام في
  الـ Worker** (قيمة تانية في نفس الفلتر) **و`Q_STATUS` في الصفحة** في نفس
  التمريرة — ومعاه بادج بيفرّق الاتنين، وإلا الرقم بيكبر بلا تفسير.

- 🟡 **مفيش «وقت الشحن» في الطابور — ومفيش ميتافيلد ليه أصلاً.** خريطة
  الميتافيلدات في `ecommoda-order-lifecycle` Step 0 مافيهاش أي حقل لوقت الشحن،
  والقاعدة ٩ بتقول إن **المدد بتتاخد من سجل D1 مش من الميتافيلد**.
  ⚠️ فالعمود المعروض دلوقتي **«الوقت منذ التغليف»** — وهو اسمه بيقول بالظبط
  اللي بيقيسه. ⛔ ممنوع يتسمّى «منذ الشحن»: التغليف بيحصل **قبل** الشحن، والرقم
  هيبقى أكبر من الحقيقة.
  **لو اتطلب وقت شحن حقيقي:** مصدره الوحيد صفوف `metafields_change` في D1
  (`value_after = 'Shipped'`)، وده محتاج **تأكيد شكل الصف** من
  `Order-Status-Updater` و`bosta-orders-shipped-scanner` **قبل** أي كود — قراءة
  D1 عابرة للأدوات مسموحة وليها عقد (`worker-builder` Step 7-ب).

- 🟡 **`read_all_orders` مش من صلاحيات التطبيق** — موروث في الستاك كله.
  **وأثره هنا أكبر من أي أداة تانية:** شوبيفاي بتخفي أي أوردر أقدم من **٦٠ يوم**
  عن تطبيق مالوش الصلاحية، **بلا أي خطأ**، وطابور «مشحون» **بيتراكم بطبيعته** —
  يعني الشحنة اللي قاعدة من شهرين (وهي بالظبط اللي الطابور اتعمل عشانها) هي
  **أول اللي بتختفي**. بند في `?action=diag` بيقولها بالاسم. القرار لأحمد.

- 🟡 **`shared/shell.css` و`shared/shell.js` نسخة تانية من هب المخزن** — الخطر
  والمخفّفات مكتوبين فوق في قاعدة الأسبقية. أي تعديل على الـ chrome يتعمل في
  الهبين في نفس التمريرة.

- ✅ **`.dco-tool*` بقى ليه تلات مستهلكين فعليين** — البند ده كان بايت من
  قبل (اتكتب وقت v1.0.0 لما الهب كان طوابير بس، وفضل زي ما هو من غير
  تحديث حتى بعد ما order-status/cod-payment دخلوا في v1.5.0). دلوقتي
  «قسم أدوات العمليات» فيه تلات كروت (`Order-Status-Updater.html` ·
  `COD-Payment-Center.html` · `Partial-Delivery.html`)، و`auto-fit` في الشبكة
  عدّى الاختبار العملي: التلات كروت دخلوا بلا أي تعديل CSS.

- 🟢 **مفيش session token موقّع** — الهوية لسه من العميل، زي كل الستاك.
  `employee` بيتبعت من العميل في كل نداء، والـ Worker بيتحقق من **السر** مش من
  هوية الموظف. المحاسبة **شرف مش إثبات**.

- 🟡 **الـ PIN بقى في مكان تالت** (الأدمن بانل · Worker التغليف · Worker الهب
  ده). الكتلة **منسوخة حرفيًا** من `ecommoda-worker-builder` →
  `references/auth-endpoints.md` — ده النمط المعتمد مش اختراع — بس التكرار
  **دايم**: أي تغيير في عقد الدخول لازم ينزل على كل النسخ.
  ⛔ وعشان كده الكتلة في `index.js` متعلّمة «ممنوع تتعدّل»: أي فرق فيها عن
  باقي الستاك = موظف بيدخل من أداة ومايقدرش من أداة تانية **بنفس الـ PIN**.

- 🟢 **`register_pin` متساب بقرار** (أحمد · 15-09-2026) — إنشاء PIN لأول مرة
  بس (مش تغيير ولا إعادة ضبط). شيله كان هيمنع الموظف الجديد من الدخول من
  المحطة دي خالص، والخطر اللي بيقفله موجود **بالظبط** في هب المخزن، فشيله من
  هنا بيقفل باب من اتنين. مكان الحماية الحقيقي الأدمن بانل.

- 🟢 **مفيش تصدير XLSX ولا تاب سجل** — الأداتين مابيكتبوش أي صف، فمفيش سجل
  يتصدّر. لو اتطلب تصدير **للطابور نفسه** (مش للسجل)، ده بند منفصل.

## بصمة المهارات

| المهارة | الإصدار وقت آخر تعديل |
|---|---|
| ecommoda-html-builder | **v6.6.0** |
| ecommoda-worker-builder | **v3.7.0** |
| ecommoda-constants | **v3.1.0** |
| ecommoda-order-lifecycle | **v1.8.0** |
| shopify-graphql-helper | **v1.1.0** |
| ecommoda-tool-migration-playbook | §13 (Promote) |

> ⚠️ **البصمة ادعاء عن الملف، مش عن الريبو** — والتلات ملفات
> (`index.html` · `ready-orders.html` · `shipped-orders.html`) و`shared/*`
> كلهم اتكتبوا على الإصدارات دي في نفس التمريرة.
> 🔴 **والتلات صفحات المدموجة (`Order-Status-Updater.html` ·
> `COD-Payment-Center.html` · `Partial-Delivery.html`) بصمتهم بصمة
> ريبوهاتهم الأصلية، مش دي** —
> منطق أدواتهم ما اتراجعش على أي مهارة في تمريرة الدمج (لا v1.5.0 ولا
> v1.10.0)، اللي اتراجع هو **الإطار** بس. ادعاء إنهم اتراجعوا على
> الإصدارات دي هيبقى **غلط**، والبصمة اللي بتدّعي مراجعة ما حصلتش أسوأ من
> مفيش بصمة.
> 🔴 **و`ecommoda-worker-builder`/`ecommoda-constants` في الجدول ده بصمة
> `index.js` كمان** (٢٢-٠٩-٢٠٢٦) — استبدال `check-log-values.mjs` بالنسخة
> المصلَّحة (بتمسك object shorthand `{ tool, type }`) وتنفيذ الحارس
> الديناميكي لقيم اللوج (الطبقة ٥ · `worker-builder` Step 7-ج): `§LOG-REG`
> (`LOG_REGISTRY` · `isRegisteredLogValue` · `noteUnregisteredLogValues`)
> جوّه `writeLog` — بيكتب الصف عادي دايمًا حتى لو `(tool,type)` مش مسجّلة،
> ويعلّم `extra._unregistered` + UPSERT صامت في `log_value_alerts`، مفيش
> رفض كتابة أبدًا. `WORKER_VERSION` بقى `1.0.1`. `log_value_alerts` جدول
> مشترك على مستوى الستاك ومتفرضش إنه اتعمل هنا لأول مرة.

آخر مطابقة: 25-09-2026 · الهب `v1.11.0`
✅ **اتقفل 16-09-2026:** إنشاء Workers الدخول والجاهز + ربط Builds +
`Build watch paths` + أسرار مجموعة `delivery_cod_ops` + OAuth + **نشر
`ready-orders-worker` v1.1.0** — كلهم متأكَّدين بالعين والأداة شغّالة حيًا.
🔴 معلّقة: **تدوير `WORKER_SECRET` على Workers الأدوات المدموجة (حاجز
تشغيل v1.5.0 وv1.10.0 — وده Promote حقيقي: السر بيعمل نسخة مش منشورة)** ·
**تسجيل مجموعة `delivery_cod_ops` في `ecommoda-constants` (بقت ستة
Workers)** · **تسجيل `delivery_cod_ops_center` في `ecommoda-constants` §7** ·
**تسجيل `tool=partial_delivery` في `ecommoda-constants` §7** (حاجز مستقل،
تفاصيله في `CLAUDE.md` بتاع ريبو `Partial-Delivery`) ·
قرار `In-Return` في طابور «مشحون»

---

آخر تحديث: 25-09-2026 — v1.11.0 (**اسم صفحة أي أداة مدموجة = اسم ريبوها
بالحرف · تطبيق `ecommoda-constants` §5**: 🔴 **الأسماء المختصرة القديمة
اتلغت** — `order-status.html` → `Order-Status-Updater.html` ·
`cod-payment.html` → `COD-Payment-Center.html` ·
`partial-delivery.html` → `Partial-Delivery.html`. صفر تعديل في منطق أو
شِل أي أداة — الأسماء بس، وكل رابط داخلي ليهم (كروت الرئيسية ·
`docs/port-standalone.py` · `docs/tools-check.mjs` · أوامر الفحص في
`README.md`) اتحدّث معاهم في نفس التمريرة. ⛔ **ومفيش تحويل من الاسم
القديم للجديد** — نفس النمط اللي اتقبل في `shipped.html → bosta-shipped.html`
بستاك المخزن: بوكمارك قديم بيدّي `404` بقرار، مش تحويل صامت.
`TOOL_VERSION` اترفع لـ`v1.11.0` كمصدر واحد للنسخة)

v1.10.0 (**دمج «التسليم الجزئي» بلا رابط خارجي ·
طلب أحمد**: 🔴 **بطاقة «التسليم الجزئي» بقت بتفتح `partial-delivery.html`
جوّه الهب** بدل رابط خارجي — نفس دخول وسر وشِل باقي المركز (مجموعة
`delivery_cod_ops` بقت **ستة Workers**). ⛔ **وعكس order-status.html/
cod-payment.html، الريبو المستقل (`Partial-Delivery`) بقى Worker وبس** —
الصفحة اللي كانت فيه اتشالت خالص، فمفيش نسخة قديمة تفضل شغّالة ومفيش
مولّد (`docs/port-standalone.py` لسه بيخدم الاتنين الأولانيين بس). ومنطق
الحذف **ما اتلمسش ولا سطر** — نفس `partial-delivery-worker` ونفس ترتيب
إلغاء الفلفلمنت → Order Edit → إعادة الفلفلمنت. `docs/tools-check.mjs`
اتوسّع لتلات أدوات واتشغّل حيًا (**106 بند عدّى · صفر فشل**)، ومعاه
`rules-check.mjs` (٩٠) و`queues-check.mjs` (٢٧٠) للتأكد إن التعديلات في
`shared/shell.js`/`index.html` ما كسرتش الطابورين ولا الأداتين التانيين —
صفر فشل في التلاتة)

v1.9.0 (**أرضية تاريخ الطابور وعمود «العميل» ·
طلب أحمد**: 🔴 **الطابورين بقوا بيبدأوا من أوردرات `01/04/2026`** — شرط
`created_at` في استعلام الـ Workers جنب شرط الحالة (`ready-orders-worker`
**1.4.0** · `shipped-orders-worker` **1.1.0**)، واللي أقدم من كده
**مابيتجابش** خالص. اليوم بتوقيت القاهرة بيتحوّل للحظة UTC بـ`Intl` مش
بيتبعت مجرّد · **والاستبعاد مُعلَن**: شيب «الطابور من 01/04/2026» جنب
الرقم الكبير و**قيمته من رد الـ Worker** مش مكتوبة في الصفحة (وبيختفي
خالص لو الرد ما رجّعهاش) ·
🔴 **وعمود «العميل» بقى سطرين بالكتير** — بنط أصغر وحشو أضيق وقص بـ`…`،
**بلا أي توسيع للعمود**: الاسم الخماسي كان بيلفّ على ستة سطور والصف يبقى
ضعف ارتفاع جاره. والاسم الكامل في `title` وفي البحث وفي التصدير ·
و`ready.min` بقى `1.4.0` و`shipped.min` بقى `1.1.0` · وفحص المتصفح بقى
**٢٧٠ بند**)

v1.8.0 (**مربعات «موقع الشحنة» وسطرا التاريخ ·
طلب أحمد**: 🔴 **خلية التاريخ بقت سطرين بالظبط** في العمودين وفي
الطابورين — سطر للتاريخ **بأيقونته معاه** وسطر للبادج (الأيقونة كانت
بتنزل لوحدها لأن النص أعرض من جوّه العمود بـ٥px، والحل **تلات تعديلات
مع بعض**: `nowrap` + حشو أضيق + بنط أصغر بدرجة — لأن `nowrap` لوحده كان
بيوسّع العمودين ويخلّيهم يفترقوا تاني) ·
**وبند الفاضي في قايمة فلتر «موقع الشحنة» بقى `—`** بدل «— مش مسجّل»،
نفس نص الخلية بالحرف ·
🔴 **وصف مربعات «موقع الشحنة» في صندوق العدّ** تحت مربعات المندوب (صفحة
الجاهز بس): ✅ في المكتب · ⚠ المخزن · ⚠ مع المندوب · `—` — كل واحد بعدده،
**والعدّ من `of` بتاعة الفلتر نفسها**، والضغطة **بتكتب في الفلتر** مش
حالة تانية جنبه · وفحص المتصفح بقى **٢٥٩ بند**)

v1.7.0 (**تمريرة فلاتر ومقاسات · طلب أحمد**:
🔴 **قايمة فلتر «موقع الشحنة» بقت بنص البادج بالحرف** (✅ في المكتب · ⚠
المخزن · ⚠ مع المندوب) والفلترة لسه على القيمة الخام (`labelOf`) ·
🔴 **فلتر «التغليف»** بقيمتين (تم / لم يتم) في الطابورين ·
🔴 **مقاسات الأعمدة**: «العميل» −٣٥٪ · عمودا التاريخ **بنفس العرض بالظبط** ·
والباقي راح لـ«العنوان» و«ملحوظات» · **تاب «الطابور» بقى «الأوردرات»** ·
🔴 **وتاب «جرد المكتب» بقى له فلاتره هو** — خطوة ① فيها **المندوب**
(مجموعات: مناديب · بوسطة · شو روم) و**موقع الشحنة**، والنطاق بقى **مستقل
تمامًا** عن فلاتر تاب «الأوردرات» وبيتقفل مع «بدء الجرد» ·
و§MULTISELECT بقى **مكوّن واحد بنطاقات** (`q` · `aud`) بدل نسختين ·
وفحص المتصفح بقى **٢٣٣ بند**)

v1.6.0 (**تمريرة تسمية على الرئيسية + تلات
تعديلات في «قسم الجاهز للشحن»** · طلب أحمد: «الطوابير» → **«قسم الحالات
الانتقالية»** و«أدوات» → **«قسم أدوات العمليات»** · الطابورين بقوا
**«أوردرات جاهزة للشحن»** و**«أوردرات تحت التوصيل»** وتحت كل واحد **كود
حالته بالحرف** (`Ready` · `Shipped`) بخط مونو مكبّر · الأدوات بقت **«تحديث
حالة الأوردرات»** و**«تحصيل الأوردرات COD»** بلا وصف — **والاسم اتغيّر في
هيدر الصفحتين كمان** · وسطر الشرح فوق الطوابير **اتشال** ·
🔴 **وفي الطابور**: عمود **«ملحوظات»** (حقل Notes بلون بنفسجي · محتاج
`ready-orders-worker` **1.3.0**) · **«موقع الشحنة» بقى بادج** (✅ أخضر
للمكتب · ⚠ أحمر لأي مكان تاني · والفاضي محايد) · **بادج الأيام تحت
التاريخ** في العمودين · و`ready.min` بقى `1.3.0`)

v1.5.0 (**دمج أداتين تحت المركز** · طلب أحمد:
`order-status.html` (محدّث حالة الأوردر) و`cod-payment.html` (مركز التحصيل)
بقوا صفحات جوّه الهب — **دخول واحد** (`dco_session`) و**سر واحد** (مجموعة
`delivery_cod_ops`، خمس Workers) وشِل واحد · قسم «أدوات» في الرئيسية **بلا
عدّاد** · الـ Workers **ما اتلمسوش** وعايشين في ريبوهاتهم · الصفحتان
**متولّدتان** من الريبوهات الأصلية بـ`docs/port-standalone.py` لأن الروابط
القديمة فضلت شغّالة (قرار أحمد) · و`docs/tools-check.mjs` ٧٨ بند ·
🔴 **وعطلان اتصلحوا مجانًا**: إزاحة التوقيت الثابتة في أداة التحصيل (كانت
هتغلط بساعة من 29-10-2026) و**صفر مهلة** على نداءات الأداتين)

v1.4.1 (**«الفعل المطلوب» في «حالة خطأ ⛔» بقى
تلقائي** · طلب أحمد: الاستعلام عن الحالة الفعلية (S1 · S2 · الإلغاء) بيشتغل
**بمجرد «إنهاء الجرد»** بلا أي ضغطة، والزرار بقى **«إعادة محاولة الاستعلام»**
وبيظهر لو النداء فشل بس · ولسه **نداء واحد مجمّع بعد ما السكان يقف**)

v1.4.0 (**تمريرة شكل على تاب «جرد المكتب»** ·
طلب أحمد: المربعات الأربعة بأسماء وترتيب جداد (إجمالي الأوردرات · أوردرات
متبقية · حالة سليمة ✅ · حالة خطأ ⛔) و**بقت فلاتر بعد الإنهاء** (ضغطة بتفتح
قسمها وبتطوي الباقي) · **التلات قوايم بقت جداول بنفس أعمدة تاب «الطابور»
بالظبط** — بما فيهم «موجودة خطأ» (كانت كروت) والسبب في صف تحت الصف ·
**شاشة البدء بقت سطرين**: عنوان بيقول اللي هيتجرد بعدده لكل مجموعة + تحذير
«برجاء عدم تحديث حالة أي شحنة أثناء الجرد» · عناوين الأقسام بلا الجمل
الشارحة وبلا عدّاد · **صفر تعديل في `shared/shell.js` §AUDIT-RULES وصفر
تغيير في أي رقم**)

v1.3.0 (**تاب «جرد المكتب»** في «قسم الجاهز للشحن»:
سكان لباركود الطرود وتلات أقسام للنتيجة (مظبوطة · مفقودة · موجودة خطأ بسببها
والفعل المطلوب) · النطاق = المعروض بعد الفلتر **ولقطة وقت البدء** · الجرد
محفوظ في الجلسة · تصدير XLSX بأربع شيتات · §AUDIT-RULES في `shared/shell.js` ·
`ready-orders-worker` v1.2.0 بـ`lookup_orders` و`ready.min` = `1.2.0`)

v1.2.0 (القسم الموحّد بالكامل في الطابورين:
لوحة فلاتر بأربع فلاتر اختيار متعدد + بحث + فترة سريعة · ترتيب بالضغط على
رأس أي عمود · صف «النتائج» · خط أكبر وأغمق · شيل سطر المدينة من العنوان ·
شيل بادج «ما اتغلّفش» · `—` بدل «لسه مش مسجّلة»)

v1.1.0 (تمريرة أعمدة وشكل في طابورَي الشحن:
«قسم الجاهز للشحن» بدل «طابور…» · مربعات فلترة مستطيلة زي قسم التغليف ·
شيل عنوان/شرح صندوق العدّ · جدول بشكل جدول التغليف · **عمود «العنوان»**
(محتاج `ready-orders-worker` v1.1.0) · «موقع الشحنة» بدل «عهدة الطرد» ·
شيل «القناة»/«عدد القطع»/«الإجمالي» · «نوع الأوردر» بقى قبل الأخير)

</div>
